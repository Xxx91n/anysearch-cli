{
  "schema": "anysearch/ship-gate-pathlint@1",
  "doc": "ADR-0072 (R71 T0 / D-002): registered sweep for the machine-local path lint (ship-gate step 1i). A new document type is registered ONLY by editing this file — the leg fails closed on unregistered .scratch doc dirs.",
  "roots": [
    "*.md",
    "docs/**/*.md",
    ".scratch/**/*.md"
  ],
  "scratchDocDirs": [
    "",
    "handoffs",
    "reports",
    "evidence",
    "backups",
    "drafts"
  ],
  "locatorLinePatterns": [
    "^\\s*(?:[-*]\\s*)?(?:\\*\\*)?Stack\\b[\\s：:]"
  ],
  "marker": "<!-- machine-local: <reason> @ <YYYY-MM-DD> -->"
}
