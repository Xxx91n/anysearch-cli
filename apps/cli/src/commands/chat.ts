// ans chat: interactive retrieval-augmented chat session.
// ponytail: MVP stub. Requires pi-agent-core for agent loop.

export async function runChat(args: string[]): Promise<number> {
  console.log("ans chat - Interactive retrieval-augmented chat");
  console.log("---");
  console.log("Status: not yet implemented (pi-agent-core pending).");
  console.log("Use 'ans search <query>' for immediate retrieval.");
  return 3;
}
