// ans llm: configure LLM providers (via @earendil-works/pi-ai).
// ponytail: MVP stub. pi-agent-core integration deferred.

export async function runLlm(args: string[]): Promise<number> {
  console.log("ans llm - LLM provider configuration");
  console.log("---");
  console.log("Status: not yet implemented (pi-agent-core pending).");
  console.log("See ADR-0001 for pi-ai integration plan.");
  return 3;
}
