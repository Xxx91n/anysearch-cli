save-exact=true
# Dead config, kept as the record of intent (r63 audit): npm 11 warns "unknown
# config" and ignores it; pnpm v11 reads .npmrc for auth/registry keys only.
# The working pnpm age gate is `minimumReleaseAge: 2880` in pnpm-workspace.yaml.
min-release-age=2
