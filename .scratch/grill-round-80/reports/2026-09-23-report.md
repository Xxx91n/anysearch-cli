# Round-80 收口报告 — 发布就绪轮（0.0.8 pass/fail 面四腿）

Date: 2026-09-23. Stack: `r80-grill`(xsk, 定稿) ← `r80-impl`(ykk→ntl→ust→qpm→nvo→qyt, 实施). 账本 D-001~D-005 全覆盖。所有声明附可复跑证据。

## T0 — 上游哨戒（纯证据零代码）

- `npm view @deepseek-ai/dsh-agent time version dist-tags --json`（10:11:30Z 快照）→ rc.3=2026-09-22T05:39:36Z / alpha.1=06:04:55Z / alpha.2=15:50:04Z 全闸内（2880min），出闸 ≈09-24 05:39/06:04/15:50Z；rc.2(09-10) 成熟。
- 包探针：alpha.2 携特征锚（`agent/created` payload {agent, source, signal?}、`agent/session-start` 缺席）；rc.3 无锚。→ 特征锚齐+稳定锚缺 → 非 L2 候选，action=none。
- `gh pr view 1764 --repo huggingface/transformers.js --json state,mergedAt,updatedAt,title` → OPEN，mergedAt=null，updated 2026-09-03T11:42Z（趋僵观察哨）。
- 结论行+双文件归档：`evidence/t0-watch.json`、`evidence/t0-watch-rc3-alpha1.md`。`git status --porcelain` 依赖面零脏（T0 期间仅 evidence 新文件）。

## T1 — E6 闸内执法（钉版 pnpm 11.24.0 实测矩阵）

实验场 `%TEMP%/r80-e6` <!-- machine-local: Windows env-var 路径引用（scratch 实验场） @ 2026-09-23 -->，四目录 a/b/c/v + 隔离 store/cache。逐格实录 `evidence/e6-matrix.md`（13 格 transcript）。要点：

| 格 | 命令（摘要） | 实测 |
|---|---|---|
| B 异机移植 | `pnpm install --frozen-lockfile --store-dir <fresh>`（无闸机写的 rc.3 lockfile 移植入闸环境） | **exit 1** `ERR_PNPM_MINIMUM_RELEASE_AGE_VIOLATION` 13 entries |
| B-nf CI 同款 | `pnpm install`（非 frozen）同形态 | **exit 1** 同错误 |
| B-fetch | `CI=true pnpm fetch` 同形态 | **exit 1** 同错误 |
| B-vdbr | `verifyDepsBeforeRun:install` + `pnpm run probe` | **exit 1**（auto-install 继承复核腿） |
| B-tl | 同 B + `trustLockfile:true` | exit 0（文档 opt-out 实证） |
| A-write | `minimumReleaseAgeStrict:false` + 钉闸内版 install | exit 0，ws yaml 自动写 15 excludes，verdict 记 `lockfile-verified.jsonl` |
| A1/A2 | 剥 strict+excludes 回放（温/删 node_modules+冷 store） | exit 0 `✓ passes (verified Ns ago)`——**verdict 缓存沿用** |
| A4 | A 形态 + 全新 `--cache-dir` 强制重验 | **exit 1** 15 entries——缓存载体坐实 |
| A3 | node_modules 在位 no-op | exit 0 `Already up to date`（零作业不验） |
| V-err/warn | package.json drift + vdbr:error/warn | exit 1 ERR_PNPM_VERIFY_DEPS_BEFORE_RUN（drift≠龄期）/ exit 0 warn+运行 |

**机制定论**：复核腿（11.1.3+/PR#11583）在 11.24.0 真实执法；`pnpm-cache/lockfile-verified.jsonl` 按 lockfile-hash+policy 指纹（不含 strict）缓存 pass 判定——R78 E6「回放绕过龄期闸」证伪 **revised**（缓存污染非缺腿）。

**γ 判定**：(a) 在册 lockfile 全成熟（仓内 frozen install exit 0 对照格）；(b) 回放四路全拦 + dsh 族 type-only 不入产物；(c) 腿实测覆盖。**γ 未触发 → 不阻塞**。

**裁决 (b) 引用上游**：放行依据=上游机器腿闭合；例外记档降级为 CI 断言依赖声明 → ship-gate 1v（三禁项+IgnoreMissingTime）fail-closed 兜底配置面。残余边界记档：同机宽松写入沿用 / trustLockfile:true / no-op 短路。

## T2 — 产品备货（dsh-plugin 上架）

- `apps/dsh-plugin/package.json`：private:true→false + publishConfig{access:public,provenance:true}；dependencies:{} 不变量不破。
- `release.yml`：pack 循环 4→5 包 + publish glob +`anysearch-cli-dsh-plugin-*.tgz`。
- ship-gate 1s 断言同构更新（publish-ready 形状），AGENTS.md invariant 行同步。
- **pack 拆验**（0.0.7 与 0.0.8 两态）：tarball=AGENTS.md+LICENSE+cordis.patch.yml+lib/index.{js,d.ts}+package.json，与 files 声明一致；packed manifest private:false+publishConfig 在位。
- **dsh plugin add 复验**（隔离 DSH_HOME，rc.2 宿主）：`dsh plugin add <tgz>` → profile 安装成功；`dsh --dump-config` → `# == @anysearch-cli/dsh-plugin` 层 + `mcp-anysearch` 行（serverName:anysearch/stdio/ans-mcp）就位。transcript：`evidence/t2-pack-verify.log` + `t2-pack-verify-0.0.8.log`。
- **0.0.8 sync-bump**：9 package.json + claude-plugin/plugin.json + ship-gate 1a pin 同 commit；CHANGELOG 0.0.8 段；publishing.md 新包首发节（预首发四步+allowed-actions 警示+provenance 边界+no-go 顺延）。
- `pnpm install --ignore-scripts` → Already up to date，lockfile 零脏。

## T3 — 新鲜度腿（ship-gate 1u/1v）

- 1u：CHANGELOG 当前轮（max grill-round-N）`## ` 头 rN token 断言 + `no-changelog-entry: <reason>` 豁免字段（goal.md）+ closeout-claims.json 注册面（count/path/symbol/field fail-closed、narrative warn、schema+round 锚、命令缺位即 fail）。
- 1v：E6 CI 断言依赖声明（trustLockfile:true / minimumReleaseAgeStrict:false / IgnoreMissingTime:true / 非空 minimumReleaseAgeExclude → fail）。
- 注册面 r80 8 声明全绿（重推导一致）。
- **红绿对**（临时 commit→真门→but undo，实录 `evidence/freshness-leg-dogfood.md`）：F1 无 r80 条目→fail；F2 计数 8≠7→fail；F3 空豁免→fail；F4 合法豁免→pass-exempt 续行全绿。
- **dogfooding 发现**（先记档再修）：pathlint 命中 e6-matrix 两处 env-var 路径（补 marker amend 回 ntl）；warn kind 无徽章（report() 补 warn 分支）；栈形偏差（r80-impl 误建并行 lane→but move 叠栈修正）。

## T4 — 文书收口

- ADR-0081（docs/adr/0081-*.md）：四腿一轴+γ 语义+例外五要件形+预首发路径+新鲜度腿设计+E6 三选一(b)+rejected alternatives+provenance 边界+「声明再推导」先例定级（fitness-function×executable-docs 推理级组合）+棘轮表述（豁免基线只缩不增/新豁免须 ADR 修订/回归破津贴=stop-everything/禁「冻结」）。
- registry canonical：`defer-r79-lockfile-agegate-replay`→closed_by ADR-0081；`defer-r72-dsh-plugin-npm-publish`→就绪待扳机；10 债 carried_log r80；updated=2026-09-23。
- docs/adr/index.md 重生 81 条（--check 绿）；CONTEXT.md R80 八词随 xsk 在位词核查通过；.gitignore round-80 白名单随 xsk 在位。
- WORKFLOW.md §4.2 缺位第 8 次核销——GitButler skill+全局 but 协议等价覆盖（r73-r79 先例）。

## 验收闭环（用户验收标准逐条）

| 标准 | 证据 |
|---|---|
| 编译通过 | `pnpm turbo check` → 8/8 successful, 0 failed（26.4s） |
| 打包通过 | ship-gate pack 腿 5/5 包 tarball 绿（embedding/cli/mcp/plugin/dsh-plugin 各 files 面核对）+ dsh-plugin 双态 pack 拆验 |
| 启动并测活软件进程 | `node apps/cli/dist/index.js --version`→0.0.8；`doctor`→23 passed/2 skipped/0 failed；MCP initialize（ship-gate step 8）server=anysearch v0.0.8；`dsh --version`→0.1.5-rc.2 + plugin add+dump-config 层核对 |
| 每个平台 test 闭环 | `pnpm turbo test`→13/13 tasks（dsh-plugin 13/13、store 78/78、cli/mcp/plugin/embedding/retriever/kernel 全绿）；跨 OS=native-smoke.yml 4-job CI 矩阵（栈未 push→PENDING 如实记）；WSL=kali-linux 无网络不可达（curl npmmirror 000）如实记档 |
| 完整 ship-gate | `node scripts/ship-gate.mjs --skip-matrix` 终态 run（本报告 commit 后执行）全腿绿：step 0-9 全 [pass]，0 fail/warn——5/5 包 packed、memory-eval 126/126、MCP initialize v0.0.8、fail-open boot、closeout-coverage 5/5、pathlint 0 violations |
| install 无脏 | `pnpm install --frozen-lockfile`/`--ignore-scripts` → exit 0，git status 依赖面零改 |

## 判据↔证据映射

| 判据 | 证据 |
|---|---|
| T0 三件套（transcript+结论行+观测戳+依赖零脏） | evidence/t0-watch.json + t0-watch-rc3-alpha1.md + 票间 git status |
| T1 变体矩阵+结论行+三选一落档 | evidence/e6-matrix.md（13 格）+ ADR-0081 D-003 |
| T2 pack 一致+plugin add+release diff+无版本碰撞+install 无脏 | evidence/t2-pack-verify{,-0.0.8}.log + release.yml diff（ust/qpm）+ pnpm install transcript |
| T3 新腿绿/陈旧红/豁免解析/注册面重推导一致/全绿 | evidence/freshness-leg-dogfood.md + ship-gate transcript |
| T4 ADR/registry/CONTEXT/用户动作/棘轮 | docs/adr/0081-*.md + registry diff + CONTEXT R80 词块 |
| 安全审阅（release/workflow 变更触发） | semgrep 1.174.0 --config auto scripts/ship-gate.mjs → 3 findings：2 存量 run() helper（自带豁免注释，非本轮引入）；1 新增 spawnSync(command) 已加信任边界注释——claims.json 为 git 追踪仓内文件与本门同信任域，输入面非外部 |

## 度量单行

`new-gate-assertions=3families(1u-freshness/1u-claims/1v-E6); exceptions=0(γ未触发); release-pkgs=4→5; registry-open=12→11; adr=80→81; claims-registered=8`

## 用户动作清单（外发均为用户扳机，不代行）

1. 手发 `@anysearch-cli/dsh-plugin@0.0.7`：checkout ust（publish-ready@0.0.7）`cd apps/dsh-plugin && pnpm publish`，或 `npm publish <0.0.7 tgz>`（evidence/t2-pack-verify.log 同源 tarball 可于该 commit 复打）。
2. npmjs 包页配 Trusted Publisher：owner=Xxx91n / repo=anysearch-cli / workflow=release.yml / env 空 + **allowed actions 勾 `npm publish`**（2026-05-20 起必需）。
3. 推 `v0.0.8` tag（步骤 1-2 完成后）→ release.yml 五包 OIDC+provenance。
4. 外发 transformers.js #1764 评论（观察哨降级草案，用户动作）。
5. 外发 gate draft（.scratch/grill-round-75/drafts/ 两份续挂）。
