anysearch-cli 仓库 R82 实施轮要把 packages/retriever 的 AnySearchProvider 从已下线的 REST GET /v1/search（实测 404 路由级下线）迁到 POST https://api.anysearch.com/mcp——MCP Streamable HTTP 端点（实测 initialize/tools-list/tools-call 全通、无 session-id 依赖即通、tools/call 返回 content[].text markdown 形如 ## Search Results + ### N. title + - **URL**: + snippet 段落、无 structuredContent 字段）。

已确立约束（账本+ADR 实物）：fail-open 语义不可破坏（provider 死只降级该臂不伤融合信封）；ANYSEARCH_ENDPOINT env override 必须保留（install-smoke dead-port 注入在用=http://127.0.0.1:9，本机现存值=http://127.0.0.1:20128/v1/search 指向无关 OmniRoute 网关）；@modelcontextprotocol/node@^2.0.0+server@^2.0.0 已在 monorepo apps/mcp 使用但 packages/retriever 未持；200-错形响应（如 OmniRoute 枚举形 JSON）必须按 fail 处理不得静默吞零结果；契约 SearchRequest.includeDomains=host allowlist 语义（kernel post-filter 收敛 hint）与 anysearch MCP domain=垂域类别枚举（15 域+sub_domain+typed params 经 get_sub_domains 前置）语义不同源。

问题：provider 级单次 tools/call 场景下 transport 实现选型——手写薄 JSON-RPC over fetch（initialize→notifications/initialized→tools/call 三步）vs 官方 @modelcontextprotocol SDK StreamableHTTPClientTransport——工业界对「嵌入式 provider 适配器 vs 全功能 MCP client」的选型心智模型与成熟落地惯例是什么？配套裁决点：响应映射（markdown text 解析 vs 请求 structuredContent vs 混合容错）、env override 旧值含 REST 尾径的兼容策略（剥尾规范化/错形 fail/原样直打）、max_results 由 20 clamp 到 10、MCP 无 mode 形参时 modes 声明处置、live 测活探针放 install-smoke 还是 CI test-online。请给推荐与理由，重点对比工业界成熟做法：SDK 依赖成本与供应链面、session/SSE/断线重连复杂度、降级与超时路径、手写 JSON-RPC 的已知坑（SSE 帧解析/Mcp-Session-Id 回传/Accept 头）。