# Round-63 审计报告 — 2026-09-15（审计窗口，只审不修）

审计对象：reports/2026-09-15-report.md + go-no-go-0.0.1.md（VERDICT=GO）+ T1–T8 栈。
方法：不信自述，全部硬验收在本窗口重跑（--force 破 turbo 缓存）；声明逐条仓库实物抽查；
$code-review 双轴子代理（Standards+Spec）+ 主窗口逐条复核其发现。

## 验收结论：PASS（带 findings 呈报，不替任何人追认）

报告的全部可复跑声明在本窗口独立重现，无一条核心声明失实：

| 声明 | 审计实测 | 结论 |
|---|---|---|
| turbo check 7/7 | --force 真跑 7/7（16s） | ✅ |
| turbo build 4/4 | --force 真跑 4/4（17s；import.meta→CJS esbuild 警告为存量噪音） | ✅ |
| pnpm pack×4：deps 无 @anysearch/*、peer=0.0.1+optional、Apache-2.0、repository、access public、embedding exports→dist | 逐 tarball 解包核验全中；workspace:* canonical rewrite→0.0.1 属实 | ✅ |
| dist 裸引用扫描（cli/mcp 仅 embedding、plugin 零、embedding 零 @anysearch） | 复扫一致 | ✅ |
| install-smoke 26/26 | 本窗口重跑 26/26（在线腿由 EXA 驱动；本窗口 TAVILY_API_KEY 缺席） | ✅（见 S 注） |
| ship-gate 9/9 全绿 | 重跑 9/9 exit 0，末行 ship gate green | ✅ |
| eval 126/126 fp 4a529f6fbe2096c8 | step7 同指纹重现 | ✅ |
| ratchet 真实账本 0 errors + 9 断言测试 | 独立调用 0 errors；测试 9/9 | ✅ |
| 10 隔离条目 TTL 2026-10-14 | ledger 实物：10 条、统一 2026-10-14、renewals 全 0、无裁决 | ✅ |
| 串行栈 9 提交 + SHA | 与 git log 实物一致（T8 现为 ntl+yto 两提交：终审书+报告收口） | ✅ |
| 授权边界：无 land/push/tag/publish | 本地/远程无 v0.0.1 tag；npm view 两包 E404 | ✅ |
| 快照 manifest 含 sha256 | **半真**：092816 含 {bytes,sha256}×19559；095851 为纯 size（{bytes}×19560） | ⚠️ F-6 |

## 逐项发现（缺失/弱化/跑偏）

### 需呈报用户裁定（文档/声明级）

- **A（中）publishing.md min-release-age 声明失实**：.npmrc `min-release-age=2` 对 npm 11.6.1 是 Unknown project config（warn+忽略）；pnpm 侧键名亦不符（pnpm 用 minimumReleaseAge/`minimum-release-age`，pnpm-workspace.yaml 无此键）。故"npm i -g 前两天不可见"不成立、`--min-release-age=0` 覆盖为空操作。72h 窗自验按精确版本装仍可行。该行系 scaffold 存量（77daab4），非本轮引入；spec D-006 原措辞本身带此假设——fixer 忠实实现了带事实错误的 spec。
- **B（中）README:8 `Status: 0.0.1 on npm` 超前于证据**：npm view 404。若按执行票序 land 先于 publish 完成，main 上将携带不实 status。建议改 "release in flight" 或确认 publish 完成后再落该行。
- **E（低）VERDICT 标签**：doc 写 GO，但本轮词表（CONTEXT.md:938 GO WITH CAVEATS）正是为此态所立（6 带病项 minuted）。实质内容正确（doc 自称 GO with execution preconditions），标签精度差。

### 建议硬化（可一行级返工或下轮）

- **C（低-中）ship-gate step5 bare-require 断言格式洞**：`/(?:require|import)\s*\(/` 只抓调用式；静态 `import/export ... from "@anysearch/x"` 逃逸。当前 3 app 全 CJS 产物无现实风险；任一翻 ESM 即静默失防。
- **D（低-中）ratchet 断言3 按 flag 短路而非裁决记录**：API 路径 flag⟺reviews 记录同源写入（retireEntry/longtermEntry 两处同写），故现状等价；但手改 JSON 只立 flag 可逃逸，且 longterm cap 只数 flag——reviews 里的 longterm 记录无 flag 则逃逸 cap。另 Date.parse NaN→过期检查静默假（fail-open 方向错）。
- **H（微）probe-tavily-domains.mjs:4 头注仍写 round-61 输出路径**（OUT_DIR 已改 round-62）——陈旧注释。

### 留痕观察（不阻塞）

- F-6 上表：快照 095851 为 size-only；且两快照均无文件内容副本，只能比对不能恢复——"快照"语义偏弱。
- install-smoke 双装用 --prefix 而非字面 npm i -g：语义等价且更不污染全局，视为实现改进，记字面偏差。
- embedding 实现为删除 private 字段（非 private:false）；CHANGELOG/commit 措辞与实物微差，语义等价。
- T2 auto-install-peers 实测结论仅在 T2 commit msg（"实测…自动 link 正常"），未落持久文档。
- QUARANTINE_MAX_LONGTERM 值在 ratchet.mjs 硬编码 >1（.mjs 不能 import .ts，可注释锚）。
- longtermEntry cap/unknown-id 无直接单测（经 ratchet fixture 间接覆盖）。
- apps/plugin exports.*.types→./src/*.ts 在发布 tarball 悬空（main 存量形态，非本轮回归）。
- decideOp 5 参签名 + null 哨兵（已注释说明）；install-smoke/ship-gate 重复 PEER_OK 探针与 tarball 正则。
- ADR-0064 未按文件名引用 q2/q3/q5/q8-atomcode 调研（现行 ADR 形态无 Research Sources 节，与 0063 同形；术语内联"来源："补偿）。
- npm EBADDEVENGINES 警告（devEngines pnpm vs npm）为设计内噪音。
- 报告"EXA/TAVILY key 在场"：本窗口仅 EXA_API_KEY 在场；在线腿只需其一故绿，复数声称与实物微差。

## 过程合规核查

- 栈拓扑事故+串行重排如实报告；动栈前外部快照两次（manifest 机制见上）。
- T4 refactor/docs 提交分离符合 ADR-0029；drive-by 均票内或 commit msg 声明（doctor 文案在 T2 commit msg）。
- 未执行任何授权外副作用。
- 审计侧记：本人 audit 暂存文件一度触发 ship-gate step0 clean-tree 红（移出后复绿）——不变量工作正常。
- D-005 附带项"@anysearch scope 无论 go/no-go 先注册占名"：npm view 404 只证未发布，scope 是否已占无法由此判定——悬而未决，呈报。

## 判定

报告实质可信，GO 裁决的证据链（编译/构建/打包/装测/9 步门/eval 指纹/棘轮/边界）全部独立复现。
A/B/E 三项呈报用户裁定是否修；C/D 建议硬化（进执行窗或下轮）；其余留痕。
凡修，修后须重跑同一套：turbo check/build --force、pack×4 实检、install-smoke、ship-gate、ratchet 独立+测试。
