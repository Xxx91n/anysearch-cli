# T0-L1 静态探针 transcript — @deepseek-ai/dsh-* @ 0.1.6-alpha.2（对照锚，R73 实证复核）

Date: 2026-09-22. Method: `npm pack @deepseek-ai/<pkg>@<ver>` → untar → 解包 .d.ts 提取 `interface Events` 声明合并块（消费面同法，对照基 = `evidence/t0-api-snapshot-rc2.json`）。探针面 = 6 个 Events-augmenting 包（dsh-agent/dsh-llm/dsh-session/dsh-system-prompt/dsh-tools/dsh-user-approval）；cordis 恒 4.0.2 不参与 diff。tarball 留机器临时目录不提交。
<!-- machine-local: probe tarballs unpacked under %TEMP%/tmp.416zub5p51/x/ @ 2026-09-22 -->

发布时间：2026-09-17T13:3xZ（dist-tag 历史 alpha）；R73 T2 已实证其 install 态单错 RED（`src/index.ts:87` TS2345）。本轮复核其 .d.ts 面作对照锚。

## Events 键面（vs pin rc.2，同包集）

- union 键数 27（rc.2 为 28）：**removed=[`agent/session-start`]**，added=[]。
- `agent/created` payload 变为 `{ agent: Agent; source: SessionStartSource; signal?: AbortSignal }`（rc.2 为 `{ agent }`——rc.2 里 `source` 原本挂在 session-start 上）。

## 消费事件 payload diff（rc.2 → alpha.2）

| 事件 | 判定 |
|---|---|
| agent/session-start | **REMOVED**（键消失 → 消费点 TS2345 expected-RED） |
| agent/created | payload +source +signal?（改名承接键） |
| tools/pre-execute | identical |
| tools/post-execute | identical |
| tools/result | identical |

## 家族包数

dep-closure n=18（17 dsh-* + cordis）：rc.2 基线 15 名中 dsh-code-runtime 退出 dsh-agent cone；新增 dsh-ptc-runtime / dsh-sandbox / dsh-sandbox-policy。与 R73 ledger「家族已 15→17」实录一致。

## ALARM 判定：**ALARM**（对照锚复核确认）

破坏特征全中：session-start 键消失 + created 携 source/signal。与 R73 install 态 RED 互证——L1 静态探针对此改名事件的先验预测成立（expected-RED = `ctx.on('agent/session-start')` 键不存在）。补充实测：0.1.6-alpha.1（09-15）即已同形（session-start ABSENT + created 携 source/signal）——改名落地起点=alpha.1 而非 alpha.2。

方法学注记：本 transcript 由 L1 静态探针产出（npm pack 不走 pnpm resolver，不受 minimumReleaseAge 管辖——闸内合法探测闸门外版本，非 bypass）。
