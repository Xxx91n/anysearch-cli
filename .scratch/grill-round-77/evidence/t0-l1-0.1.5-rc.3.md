# T0-L1 静态探针 transcript — @deepseek-ai/dsh-* @ 0.1.5-rc.3（next 线）

Date: 2026-09-22. Method: `npm pack @deepseek-ai/<pkg>@<ver>` → untar → 解包 .d.ts 提取 `interface Events` 声明合并块（消费面同法，对照基 = `evidence/t0-api-snapshot-rc2.json`）。探针面 = 6 个 Events-augmenting 包（dsh-agent/dsh-llm/dsh-session/dsh-system-prompt/dsh-tools/dsh-user-approval）；cordis 恒 4.0.2 不参与 diff。tarball 留机器临时目录不提交。
<!-- machine-local: probe tarballs unpacked under %TEMP%/tmp.416zub5p51/x/ @ 2026-09-22 -->

发布时间：2026-09-22T05:39Z（dist-tag `next`）；龄期闸 2880min 内，出闸 ≈2026-09-24 05:39Z。

## Events 键面（vs pin rc.2，同包集）

- union 键数 28 ≡ rc.2（**零漂移**：added=[] removed=[]）。
- `agent/session-start` **PRESENT**：`payload: { agent: Agent; source: SessionStartSource }` ≡ rc.2。
- `agent/created` PRESENT：`payload: { agent: Agent }` ≡ rc.2（rc.2 起两键并存）。

## 消费事件 payload diff（rc.2 → rc.3）

| 事件 | 判定 |
|---|---|
| agent/session-start | identical |
| agent/created | identical |
| tools/pre-execute | identical |
| tools/post-execute | identical |
| tools/result | identical |

## 家族包数

dep-closure（直依根 cordis/dsh-agent/dsh-llm/dsh-system-prompt/dsh-tools）n=16 ≡ rc.2（15 dsh-* + cordis 4.0.2），无新成员。

## ALARM 判定：**无 alarm**

破坏特征（`agent/session-start` 移除 + `agent/created` payload 增 `source`/`signal`）**未出现在 rc/next 线**。改名仍只在 alpha 线。rc.3 是 0.1.5 维护线发版，非采纳候选（无 ALARM → 不进 L2 排队）。

方法学注记：本 transcript 由 L1 静态探针产出（npm pack 不走 pnpm resolver，不受 minimumReleaseAge 管辖——闸内合法探测闸门外版本，非 bypass）。
