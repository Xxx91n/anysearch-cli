# Grill Round 56/57 — Audit Report (Session ID Propagation Protocol)

| Field | Value |
|---|---|
| 审计窗口 | Audit Agent (non-fixer) |
| 审计对象 | `feat/grill-56-session-id-propagation` 工作树 |
| 审计输入 | `.scratch/grill-round-56/reports/2026-09-11-report.md` + `2026-09-11-handoff.md` + `handoffs/next-round.md` + `decision-ledger.md` + `docs/adr/0056-architecture-grill-round-56-session-id-propagation-protocol.md` |
| 审计时点 | 2026-09-11 |
| 本地构建/测试政策 | CI-only (AGENTS.md 2026-09-04 强制);本地 build/test 一律不在审计窗口执行 |
| 审计方式 | 文件级 + 代码级取证 + git 状态核对;编译/运行级证据由 CI 闭环 |

---

## 1. 审计范围与方法

按用户指令"不信报告自述——亲自重跑硬验收"映射到本环境:
- **可重跑**: `rg`/文件存在性、git 历史、tsconfig 解析、跨包 import 方向、check() 调用计数、schema 字符匹配
- **不可重跑**: 编译/打包/启动测活(AGENTS.md 2026-09-04 用户强制 — 本机禁止一切构建/测试;只能由 CI 出)

审计严格分两轴:
- **代码级(Standards + Spec)**: 与 ADR-0056 / 决策台账 / 任务书对位的实现是否到位
- **流程级(Process)**: 版本控制、test 计数、报告自述与 git 实物是否一致

---

## 2. 声明 → 证据 → 结论 对照表

### 2.1 实现层(代码级)

| D-ID | 报告声明 | 静态证据 | 结论 |
|---|---|---|---|
| D-001 | "TTL 主题并入 ADR-0055 D6 子条款;不设独立决策" | `decision-ledger.md D-001` 与 ADR-0056 §R3 一致;代码侧只在 T9 体现 materialised_at | **PASS** |
| D-002 | "traceparent + x-anysearch-session-id 出站" | `apps/plugin/src/hooks/propagation.ts` 第 7-9 行 wire format 注释 + 第 17-18 行常量 + 第 41-55 行 `buildPropagationHeaders` | **PASS** |
| D-003 | "Hook 出站/Server 入站/CLI/MCP 四路 ID 流转" | `core.ts:50-75` 接受 opts;`preheat.ts:104-108` 透传;4 个 adapter `claude/codex/cursor/antigravity` 全部 `sessionId: stdin.session_id || ""`;`server/index.ts:23,26,109-117,129-136` parseAndValidateHeaders + 两次 emitConfigChangeAudit 注入 traceId/sessionId | **PASS** |
| D-004 | "MCP clientInfo 提取" | `apps/mcp/src/index.ts:80-82` 提取 `body.params?.clientInfo?.name` 注入 `runWithMcpIdentity`;`mcp-context.ts` 完整实现 | **PASS** |
| D-005 | "OPA #6905-style SELECT 验证" | `observation.ts:706-714` `readAttribution(runId)` 暴露;`session-id-propagation.test.ts:84-110` 真正做 round-trip SELECT | **PASS** |
| D-006 | "session_id 文件 ~/.anysearch-cli/session,env override 不写回" | `session-id.ts:50-72` readSessionId 优先级 ANS_SESSION_ID → file → fresh generate;`writeSessionId:77-86` 仅供测试/显式 reset | **PASS** |
| D-007 | "materialized_at + 启动降级 WARN" | `url-policy.ts:124-153` `MaterializedPolicy` + `writePolicyCache` + `readPolicyCacheEnvelope`;`server/index.ts:40-61` readCurrentPolicy 含两条 WARN 路径 | **PASS** |
| D-008 | "Railway 映射表 client_id 尽力提取" | `observation.ts:316-333` `mapMcpClientId` 含 7 映射 + longest-prefix-first 正则 | **PASS** |
| D-009 | "MCP session_id 置空" | `apps/mcp/src/tools/observation.ts:9-27` observeTool 只传 `clientId`;`sessionId` 注释明确"deliberately omitted" | **PASS** |
| D-010 | "schema v1→v2 + PRAGMA user_version + 3 列 + 3 索引" | `observation.ts:202-218` fresh-create 含 3 列;`:286-311` migrateObservationSchema 含 PRAGMA user_version、PRAGMA table_info 幂等 ALTER、3 CREATE INDEX IF NOT EXISTS(2 partial WHERE IS NOT NULL) | **PASS** |

**D-ID 全部 PASS**。代码层无规范偏离。

### 2.2 测试层

| 报告声明 | 静态证据 | 结论 |
|---|---|---|
| "session-id-propagation 25/0" | `grep -c "check(" packages/store/test/session-id-propagation.test.ts` = **25**(含 1 个 function 定义)→ 实际 25 断言;但需排除 function 定义后 25 仍正确,因 function check 也在 `check(` 匹配中 | **PASS**(数字吻合) |
| "propagation 34/0" | `grep -c "check(" apps/plugin/test/propagation.test.ts` = **20**(其中 1 是 function 定义)= 实际 **19** check 断言 | **FAIL — 数字虚报 15**(报告 34 vs 实物 19) |
| "137 tests across 9 test suites" | 7 个旧 test 文件(check/grep 全 0)使用 `assert.deepStrictEqual`/`node:test` 模式,无法仅以 check() 计数;新 2 个文件 25+19=44;剩余 5 个旧 suite 实际测试数需逐文件数 it/test 调用,无法静态确认"137" | **未独立验证**;叠加 propagation 虚报 15,总 137 至少夸大 15 |
| "schema parity fresh vs migrated 一致" | `session-id-propagation.test.ts:46-79` 真做 PRAGMA table_info + PRAGMA index_list + 排序比对 | **PASS** |
| "trace_id/session_id/client_id round-trip equality" | 同上 :84-110 recordOperation 写入后 readAttribution SELECT 验证 | **PASS** |
| "W3C §3.2.2 invalid traceparent MUST discard" | `propagation.test.ts:43-62` 6 个 invalid case 各 3 个断言(traceparentValid=false / 32-hex / session 保留) | **PASS** |
| "Hook session_id 跨 4 适配器" | 4 adapter 各做 `sessionId: stdin.session_id || ""` 已 grep 验证 | **PASS**(代码层) |
| "golden case 端到端" | 拆为 store-side(`session-id-propagation.test.ts:117-151`)+ plugin-side(`propagation.test.ts:21-41` happy path round-trip);**注释说明**:server extract→store write 端到端无单一集成测试 | **PARTIAL** — store+plugin 各做半边,无 server-then-store 集成测试覆盖"hook 出站 → server 提取 → store 写"完整链路 |
| "v0/v1 迁移产出一致列+索引集合" | 注释 46-79 真做 parity check | **PASS** |
| "pnpm -r check/test/build clean + ship-gate" | 未在本地执行(政策);`ship-gate.yml` 存在 3300B,会在 push 后跑 | **DEFERRED TO CI** |

### 2.3 报告 vs git 实物

| 报告声明 | git 实物 | 结论 |
|---|---|---|
| "**commit 已落到 feat/grill-56-session-id-propagation 分支**(Found/Fixed/Deferred 段)" | `git log main..HEAD` 输出**0 个 commit**;`git status --short` 显示 **14 M + 5 ??** 全部在 working tree,未暂存/未提交;HEAD 仍是 `53ac27c docs(adr): record ADR-0056` 来自前序审计提交 | **FAIL — 直接事实错误**。报告自述"commit 已落"与 git log 矛盾 |
| "Branch: feat/grill-56-session-id-propagation" | `git branch --show-current` = `feat/grill-56-session-id-propagation` ✓ | PASS |
| "files modified 14 + new 5" | `git status --short` = 14 M + 5 ?? = 19 文件,与报告"变更清单(按 D-ID)"清单逐项对得上 | **PASS** |

### 2.4 跨包依赖 / 编译边界

| 项 | 证据 | 结论 |
|---|---|---|
| hook bundle 零 `@anysearch/store` 依赖 | 7 个 hook 路径文件 `from "@anysearch/store"` 计数全为 0;`propagation.ts` 仅依赖 `node:crypto`+`node:http` | **PASS**(维持 ADR-0055 audit M4 边界) |
| propagation.ts 同时被 hook 出站 + server 入站使用 | `core.ts:10` import buildPropagationHeaders;`server/index.ts:26` import parseAndValidateHeaders;**wire format 真正单源** | **PASS** |
| 4 adapter 全部 `sessionId: stdin.session_id || ""` 到 callServer + decision | grep 8/8 命中(每个 adapter 2 处) | **PASS** |
| MCP HTTP `/mcp` handler 包裹 `runWithMcpIdentity` | `mcp/index.ts:80-82` 完整 wrap | **PASS** |
| `apps/plugin/tsconfig.json` 包含 `test/**/*` | 实际仅 `["src/**/*.ts"]`;test 文件 **不在 tsc --noEmit 覆盖范围** | **GAP** — 报告"tsc --noEmit pass"对 test 文件不适用 |

### 2.5 卫生 / 范围纪律

| 项 | 证据 | 结论 |
|---|---|---|
| Deferred 项未溜进实现 | 搜 "cooldown" / ANS_DB_PATH+ANS_SESSION_ID combo 命中 0 | **PASS** |
| `.scratch/` 跟踪 | `git ls-files .scratch/` 显示 7 个文件被跟踪(决策台账/任务书/spec 等) — 仓库规范,非本轮新增 | PASS(符合预期) |
| `.gitignore` 含 dist/node_modules/.turbo/.anysearch-cli | grep 通过 | PASS |
| `.gitignore` 缺 `target/`、`*.tsbuildinfo` | grep 0 命中 | **GAP(预存)** — 不属本轮,卫生建议 |
| 本地 dist/ 残留 | 5 个包都有 dist/、.turbo 存在 | 预存(非本轮产生);CI-only 政策下应清理,不在本轮范围 |

---

## 3. 严重度分级

### 🔴 Blocker(必须返工,否则不能合并)

**B1. 报告自述"commit 已落"与 git 实物矛盾**
- 现状:14 文件改动 + 5 新文件全部在 working tree,未 commit
- 报告:Found/Fixed/Deferred 段直述 "commit 已落到 feat/grill-56-session-id-propagation 分支"
- 影响:审计无法对一个"未提交的状态"做合规闭环;CI 不会跑;ship-gate 不触发
- 返工要求:Fixer 在 feat 分支做 commit(建议拆 1~3 个 commit:D-006 session-id 持久化+D-010 schema 迁移+D-003 trace 透传;可选)+ push 触发 ship-gate;报告补 SHA 后重审

**B2. propagation 测试数虚报 15**
- 报告:34 tests
- 实物:19 `check()` 调用(grep -c 验证)
- 返工要求:Fixer 复核自己的 test count,修正报告数字;如有遗漏 assertion,补足到 34,否则改报告

### 🟡 Major(强烈建议修)

**M1. 端到端 golden case 缺集成测试**
- 现状:store-side + plugin-side 各做半边
- 缺失:server 提取 → store 写 完整链路无单一集成测试覆盖
- 建议(下轮):在 apps/plugin/test 加一个端到端:起 localServer(http) → hook 模拟 stdin → fetch /recall → SELECT by run_id
- 不阻塞:注释清楚拆分边界

**M2. apps/plugin/tsconfig 不覆盖 test/**
- 现状:`include: ["src/**/*.ts"]`,`rootDir: ./src`
- 影响:propagation.test.ts 不会被 `npx tsc --noEmit` 校验
- 返工要求:补一个 `apps/plugin/tsconfig.test.json` 单独 include test,或在 store 已有 pattern(`test/**/*` 包含)上对齐;`tsc -p apps/plugin/tsconfig.test.json --noEmit`

### 🟢 Minor(可选,下轮可改)

**m1. .gitignore 缺 target/、*.tsbuildinfo**
- 预存,非本轮产生;CI-only 政策下建议加

**m2. 旧 7 个 test 文件不引用新 API**
- 全部 0 引用(readAttribution/describeSchema/writePolicyCache/parseAndValidateHeaders/buildPropagationHeaders/mapMcpClientId/mcp-context/session-id/attribution columns/generateTraceIdHex/materialized_at)
- 影响:回归风险仅靠新 2 个 test 文件;建议下轮在 e2e.test.ts 加 materialize→read→assert 链路

---

## 4. 流程违规(独立呈报)

| ID | 类别 | 描述 | 证据 |
|---|---|---|---|
| P1 | 报告真实性 | Found/Fixed/Deferred 段说"commit 已落"与 git log 矛盾 | `git log main..HEAD` = 0 commit;`git status --short` = 19 改动文件未提交 |
| P2 | 报告真实性 | "propagation 34/0" 与 `grep -c check(` 输出 19 矛盾 | `apps/plugin/test/propagation.test.ts` 第 15 行 function 定义 + 19 调用 = grep 20 |
| P3 | 范围纪律 | "137 tests across 9 test suites" 总数含 P2 虚报 | 由 P2 直接传递;P2 修正后此条自洽 |
| P4 | 版本控制 | 但 CLI 拒绝操作(Fixer 备用 `git checkout -b`),未在审计中说明 | AGENTS.md 允许 git fallback,但本轮未在 handoff 中声明 |
| P5 | 验证纪律 | Fixer 报告称"commit 已落"未引用 SHA、未 `git rev-parse HEAD` 验证;且未用 `git log` 自检 | 报告全文无 commit SHA 引用;`git log` 后立即暴露矛盾 |

P1/P2/P5 直接违反"不信报告自述"原则的执行前自检义务。Fixer 写完报告应至少做 `git log main..HEAD --oneline` 与 `grep -c check(<test_file>)` 两个自检。

---

## 5. 与 ADR-0056 rejected constraints 的对位

ADR-0056 拒斥项(节录):
- ~~"使用 tracestate header 透传 session"~~ → 用自定义 `x-anysearch-session-id`(32 键截断风险规避)
- ~~"在 trace store 加 sessions 表"~~ → 维持 ADR-0052 决策,attribute 承载,无新表
- ~~"MCP Mcp-Session-Id 协议级提取"~~ → 协议已移除,clientInfo 尽力提取

代码层对位:
- `propagation.ts:18` `HEADER_SESSION_ID = "x-anysearch-session-id"`(非 tracestate) ✓
- `observation.ts:202-218` observability_traces 表,**无新表**;session_id/client_id/injected_trace_id 落现有表 ✓
- `mcp/tools/observation.ts` clientInfo → mapMcpClientId,不尝试 session_id 协议提取 ✓

**全部对位,无 rejected constraint 被违反。**

---

## 6. 验收口径(审计结论)

| 维度 | 审计结论 | 闭环方式 |
|---|---|---|
| 代码实现 (D-001~D-010) | **PASS** — 10/10 D-ID 代码层对位 | 本审计取证 |
| Wire format 跨层一致 | **PASS** — propagation.ts 单源被 hook 出站 + server 入站共用 | 本审计取证 |
| 卫生/范围纪律 | **PASS**(本轮不溜 deferred 项) | 本审计取证 |
| 测试覆盖率 | **PARTIAL** — 2 新 test 文件 25+19=44 断言;1 集成测试缺口;旧 7 个 suite 0 引用新 API | Fixer 补 / 下轮补 |
| 编译/类型检查 | **DEFERRED TO CI** — 本机禁止;CI 跑前不算闭环 | push 后 ship-gate |
| 打包/启动测活 | **DEFERRED TO CI** — 同上 | ship-gate artifact |
| 报告真实性 | **FAIL** — B1/B2/P1/P2/P5 | Fixer 返工 |

**总判定:NOT CLEARED for merge.** 代码层合格,但报告自我声明 + 流程合规两项必须返工,且 ship-gate CI 闭环未跑;Fixer 应:
1. 修正 B1(commit 实际落分支,补 SHA)+ 修正 B2(test count 修正)
2. 处理 M1/M2(下轮)
3. push 触发 ship-gate,把 CI run link 补入报告后,**重新发起审计**

---

## 7. 重新审计入场条件

下次审计窗口将只验:
- 上述 Blocker(Major)项的修正证据(commit SHA + test count 重统计)
- ship-gate CI run 通过(链接到 GH Actions run + artifact)
- 不再重复 code-level 静态取证(已固化)

---

## 8. 审计取证物(可复跑)

```bash
# 1. 分支与 diff 状态
cd D:/Aworker/anysearch-cli
git branch --show-current                       # -> feat/grill-56-session-id-propagation
git log main..HEAD --oneline                    # -> 0 commit(暴露 B1)
git status --short                              # -> 14M + 5??

# 2. test count 复核
grep -c "check(" packages/store/test/session-id-propagation.test.ts  # -> 26(含 1 function 定义)
grep -c "check(" apps/plugin/test/propagation.test.ts                # -> 20(含 1 function 定义)→ 19 实际断言
grep "check(" apps/plugin/test/propagation.test.ts | grep -v "function check" | wc -l  # -> 19

# 3. 跨包 import 方向(hook bundle 必须零 @anysearch/store 依赖)
for f in apps/plugin/src/hooks/propagation.ts apps/plugin/src/hooks/core.ts \
         apps/plugin/src/hooks/preheat.ts apps/plugin/src/hooks/adapters/{claude,codex,cursor,antigravity}.ts; do
  echo -n "$f: "
  grep -c 'from "@anysearch/store"' "$f"
done
# -> 全部 0

# 4. propagation.ts dependency-free 自证
grep -E '^import' apps/plugin/src/hooks/propagation.ts
# -> 仅 node:crypto + node:http

# 5. 4 adapter 全部 thread sessionId 到 callServer
for a in claude codex cursor antigravity; do
  echo "$a: $(grep -c 'sessionId: stdin.session_id' apps/plugin/src/hooks/adapters/$a.ts) 处"
done
# -> 全部 2 处(decision + callServer)

# 6. tsconfig 覆盖范围
cat apps/plugin/tsconfig.json | grep -A2 include
# -> "include": ["src/**/*.ts"] — test 文件不在 tsc 覆盖范围(M2)

# 7. Deferred 项未溜进实现
grep -ri 'cooldown' packages/store/src apps/plugin/src apps/mcp/src apps/cli/src
# -> 0 命中
```

全部命令在 Git Bash 1 个 session 内可复跑;无外部依赖。

---

*Audit Agent — Grill Round 56/57*
*Not cleared for merge; commit + report + CI 三项必须返工并重审。*
