# Grill Round 61 — 交付报告（2026-09-14）

主题：docs 域对域外提问 abstain 收口（bc0001 升正票）+ README 用户向重写。串行五票 T1→T5 全部独立提交，无中间态合并。

## 提交栈（grill-61-docs 之上，git log 实测哈希）

| 票 | GitButler 分支 | 提交 | git hash |
|---|---|---|---|
| T1 能力协商 pre-filter | r61-t1-prefilter | nmo | c595792 |
| T2 kernel 权威 post-filter | r61-t2-postfilter | ktl | c7bf545 |
| T3 abstain 呈现契约 | r61-t3-present | mml | 7da70fc |
| T4 golden 判据+ship-gate 接线 | r61-t4-golden | rsn | 8344941 |
| T5 README 用户向重写 | r61-t5-readme | lsk | f1f002c |
| 修正(审计计数精度) | r61-t3-present 之上 | put | a355ccb |
| 收口(报告+交接) | r61-closeout | rrz | 5e82c8f |

## T1 — 能力协商式 provider pre-filter（D-002 前半）

- `contract.ts`: `SearchRequest.includeDomains?: string[]` + `SearchProvider.domainFilterSupported?: boolean`。
- tavily 下发 `include_domains` + 显式 `include_domains_mode:"filter"` 硬模式；exa 下发 `includeDomains`；anysearch 显式 `readonly domainFilterSupported = false`（诚实降级，不伪造）。
- 禁项遵守：无 `site:` query-rewrite；不支持 provider 不硬造过滤。
- 证据：`cd packages/retriever && node --import tsx --test test/domain-filter.test.ts` → 11 passed, 0 failed；`npx tsc --noEmit` → 0。
- 判据5 探针：`scripts/probe-tavily-domains.mjs` 可复跑；本机 TAVILY_API_KEY 未设置 → 四臂全部诚实记 SKIPPED（`.scratch/grill-round-61/tavily-probe-ledger.md`）。复跑：`TAVILY_API_KEY=tvly-... node scripts/probe-tavily-domains.mjs`，D 臂加 `PROBE_TAVILY_RESEARCH=1`。

## T2 — kernel 权威 post-filter + 双审计事件 + 第一类 abstain（D-002/D-003）

- `engine.ts`: `adjudicateUrlPolicy` 复用 `shouldAllowUrl`（retrieved 源语义：deny 优先、allow 含子域、无 user-source 早放）；到达集合在 RRF/MVSS/归因/输出之前过闸；`metadata.abstain` 标记 `{abstain:true, reason:domain_filter_empty, domain, preFiltered, postFiltered, gate}`。
- `composition.ts`: `urlPolicy` 请求时解析（`createDomainReloader` lazy-mtime + `resolvePolicyFromSchema`，无缓存快照）；`domainName` 随引擎。
- 双事件：`retrieval.domain_filter.pre`（capability/sent/degraded/allow/deny/policy_version）+ `retrieval.domain_filter.post`（pre/post/dropped/outcome）；`anysearch.outcome` 独立维度（answer|abstain|error），abstain 不混入 error。
- 证据：`cd packages/kernel && node --import tsx --test test/domain-filter.test.ts` → 31 passed；e2e 实测（ANS_DOMAIN=docs，真实 exa/tavily）：observability_spans 最新 trace 显示 `pre` 事件 sent=[tavily,exa] degraded=[anysearch]，`post` 事件 pre=20 post=19 **dropped=1**（post-gate 真拦到一条域外泄漏），span 属性 `anysearch.outcome=answer`；随后修正 `dropped` 计数精度（gateDropped/gateSurvivors 分离，pre=post+dropped 算术闭合，提交 put/a355ccb）；cold 域（`.scratch/grill-round-61/narrow-domains/cold.toml`，allowlist=nonexistent.invalid）跑同查询 → `anysearch.outcome=abstain`、status 保持 `ok`。

## T3 — abstain 呈现契约（D-003）

- CLI：`apps/cli/src/commands/search-abstain.ts`（纯叶模块，CJS 可达可测）导出 `searchExitCode` + `formatAbstainLine`；单行输出 `abstain: no results within allowed <domain> domain(s) (pre-filtered N, post-filtered M, gate <pre|post>)`；默认 exit 0，`--fail-on-abstain` → 专用 exit 3；`--json` 输出含 `abstain` 字段；顺带修复 `--flag` 泄进 query 的既有 bug。
- MCP：`search_web`/`research_web` 的 `structuredContent.abstain`，`isError` 保持 false；`observeTool` 回调现收到 span（检索事件进 MCP trace）；`server.ts` 接通 `ANS_DOMAIN`（与 CLI 同一 env 契约）。
- 证据：`cd apps/cli && node --import tsx test/abstain.test.ts` → 15 passed（含真实 dist 子进程实测：冷域 abstain exit 0、`--fail-on-abstain` exit 3）；`cd apps/mcp && node --import tsx test/abstain.test.ts` → 9 passed（真实注册 handler 进程内调用断言 structuredContent 契约）。

## T4 — golden 判据 1-4 + ship-gate 接线

- eval-looks.json 新增三条：`docs-g0012`（域内 must-hit 成对锚，防过度弃答）、`docs-g0013`（降级 stub-provider abstain 臂，锚 kernel 测试）、`docs-g0014`（冷域零结果 abstain，锚 cold.toml fixture）。
- `docs-golden.ts`：非 docs 域条目仅允许 verdict=abstain 且无 mustHit；crossCheck 对冷域豁免 docs allowlist 子集校验。
- `eval-looks.coverage.json` 诚实重计数（14 条；negative:abstain 1→3）。
- `eval-docs-golden.test.ts` 新增 8b 段断言四判据锚；`scripts/ship-gate.mjs` 新增 1n 静态块（双闸契约 token + 判据锚 + 探针存在性）。
- 证据：`cd packages/store && node --import tsx test/eval-docs-golden.test.ts` → 44 passed；`node scripts/ship-gate.mjs --quick` → 1n 块 `[pass]`。

## T5 — README 用户向重写（D-004）

- 结构：诚实 0.0.1 定位（未上 npm）→ Requirements → Quickstart（每条命令实测）→ Domains & abstain（双闸+退出码+审计事件）→ provider 能力矩阵 → MCP server → Known Limitations（5 条：tavily AbortSignal、anysearch 无预过滤、golden 执行器 deferred、macOS 未手验、LLM 键）→ 贡献者压缩链接 → ADR index 原样保留。
- 实测命令证据：`node apps/cli/dist/index.js doctor` → 22 passed/2 skipped/0 failed exit 0；`--help` 列出 17 命令+env；`search --json` 结构化输出含 abstain 字段；`node apps/mcp/dist/index.cjs --transport http --port 3099` → `GET /health` 200 `{"status":"ok","server":"anysearch-mcp","version":"0.0.1"}`；`gen-adr-index --check` → up to date (62 ADRs)。
- 顺手修漂移 bug：doctor/auth 曾检查 `ANS_API_KEY`，但 provider 实际读 `ANYSEARCH_API_KEY`（verify-observation.mjs 的 env scrub 早已按后者写）——已统一为 provider 读的那个。

## 最终验收（验收标准原文逐条核对）

> 编译通过：`turbo run check` 7/7（ship-gate step3）。
> 打包通过：`pnpm pack` 5 包 tgz + `npm install --prefix` 安装验证（step4/5）。
> 启动并测活：MCP stdio initialize 绿（step8）+ HTTP /health 200（实跑）。
> 每个平台 test 闭环：retriever 11+19+8/19/8… 全绿、kernel 63（含域过滤 31）、store golden 44、cli e2e 11 + abstain 12（turbo 下 live arm 无 key 诚实 SKIP）+15（含 live arm 本机跑通）、mcp 14+9、plugin 全绿；ship-gate 9/9。

## 负向约束核对

无 site: 注入、无伪造过滤、abstain 默认 exit 0、结构化契约非 audit-only、断言锚结构化 verdict 非 regex、README 无未验证命令、ADR 索引未删、T1→T5 逐票闭环、npm 未发布。

## 遗留（诚实挂账）

- TAVILY_API_KEY 缺席 → 判据5 四臂 SKIPPED，待 key 到位后跑 `scripts/probe-tavily-domains.mjs` 回填账本。
- cold 域 abstain 实跑后进程尾部偶现 libuv `UV_HANDLE_CLOSING` 断言（Windows Node + sqlite 句柄退出怪癖），exit code 不受影响——pre-existing，非本轮引入。
- readme-crafter / beautify-github-readme skills 在本机不存在（任务书引用路径未安装）——按其意图（用户向、实测证据、诚实限制）执行，已如实记录。
- 栈未 push/PR（未明令），五分支在 GitButler workspace 内独立可回滚。

