# Grill Round 72 — 实施报告：DeepSeek Harness（dsh）宿主适配

Stack：分支 `r72-grill`（GitButler 栈，与 `r71-audit` 等并行互不写），base `c4ced36a`。轮内提交：`uzt` (d72b1e97 @ 2026-09-19) grill定稿 → `tqq` (ebf7feb3) T0 spike → `yxo` (ee5bbbc7) T1/T2 实施+验证 → `uyt` (20a05b70) ADR index → `pxv` (019466b3) gitignore → `nnn` (2009b3ad) Stack行修复 → `zmu` (5494c91b) T2证据+收口文档（含本报告与 closeout）。
裁决账本：`.scratch/grill-round-72/decision-ledger.md`（D-001~D-004 全落地）。ADR：`docs/adr/0073-architecture-grill-round-72-deepseek-harness-host-adaptation.md`。

## 验收标准对照

> 用户：「编译通过、打包通过、启动并测活软件进程；每个平台都要有 test 闭环，避免只引入却没做到。」

- 编译通过：`turbo run check` 8/8 包绿（含新包 tsc --noEmit）——命令 `pnpm turbo run check`，输出 `Tasks: 8 successful, 8 total`。
- 打包通过：`pnpm pack` 出 `anysearch-cli-dsh-plugin-0.0.6.tgz`（lib/index.js + lib/index.d.ts + cordis.patch.yml + LICENSE + package.json）；ship-gate step4 全 8 包打包绿。
- 启动并测活：真 dsh 进程（@deepseek-ai/dsh@0.1.5-rc.2）headless 一次性 turn 干净退出 `PROBE_TURN_COMPLETE`；web profile 组合体起 `127.0.0.1:3080` 服务并完成 headless 驱动 turn。证据：`.scratch/grill-round-72/evidence/t1-boot5-real-bundle-e2e.log` + boot10 web 实录（见下 T2）。
- 每平台 test 闭环：apps/dsh-plugin mock-ctx 单测 13/13 绿（node:test + tsx）；turbo test 全工作区执行（dsh-plugin 13/13；apps/plugin 10/10）。

## D-004 四段收口对账

### (i) spike 段 —— 报告实物已存在

`.scratch/grill-round-72/reports/spike-report.md`：9 探针逐项 PASS/FAIL/RESHAPE + transcript 锚点 + #8 blocking 判定（PASS——bundle cordis.patch.yml insert mcp-anysearch 行→真 ans-mcp 桥接+model 调 mcp__anysearch__search_web 端到端真搜索 10 条回）+ Phase-2 **GO**。证据：`.scratch/grill-round-72/evidence/t0-boot*.{log,txt}` + `t0-llm-stub-requests.log` + `t0-probe-plugin/`。

### (ii) 落地段 —— apps/dsh-plugin diff + mock 单测 + Phase-1 文档

- 新包 `apps/dsh-plugin`（`@anysearch-cli/dsh-plugin`）：src/index.ts（五面 hooks，约 200 LOC）、cordis.patch.yml（- insert: 插件行 + mcp-anysearch 桥行整行复述）、package.json（private:true、type:module、dsh.bundle.patch 声明、dependencies 无 @deepseek-ai/*、devDeps 类型耦合）、tsconfig.json、test/dsh-plugin.test.ts（13 用例）、AGENTS.md。
- mock-ctx 单测：`pnpm -C apps/dsh-plugin test` → `13 tests / 13 passed / 0 failed`（稳定 name+inject、五面挂载、routing-card、注入、d deny/ask、预热、server-down fail-open、蒸馏、索引、非 ans 工具穿透、durable 消息形）。
- Phase-1 配置行文档实证：`docs/deepseek-harness-integration.md` §Phase-1——用户 patch - insert: mcp-anysearch 行全文 + 三律记档（config 整行替换 / - id: 打缺失行仅告警 / 同 id 双 insert=loader 硬错）。spike 全 PASS 无 fallback 件。
- apps/plugin 窄口子：exports map + esbuild entries 增 `./hooks/routing-card` `./hooks/server-token`（dist/hooks/routing-card.cjs + server-token.cjs 已产出并入 pack）。

### (iii) 验证段 —— 真宿主三桶全量

**安装/配置层**（evidence: `t1-boot5-real-bundle-e2e.log` + `t2-idempotent-install.log` + `t2-dump-config-layers.log`）：
- `pnpm pack` → `dsh plugin --profile headless add <tgz>` 一步装；`--dump-config` 层核对：bundle 层 `# == @anysearch-cli/dsh-plugin` 出插件行 + mcp-anysearch 行 + `patched by` 用户层标记。
- remove + re-add 幂等：`dsh.profile.bundles` + `file:` dep 双字段登记/清理实证，同 tarball 再 add = `Already up to date` no-op。
- Phase-1-only 降档面对照：`t2-phase1-only-boot.log`；server-down fail-open 双臂：`t2-headless-serverdown.log` / `t2-bundle-serverdown.log`；tarball profile turn：`t2-tarball-profile-turn.log`。
- 升级 diff 演练：devDeps bump `0.1.5-rc.2 → 0.1.6-alpha.2` → `tsc --noEmit` 绿 → 回钉 rc.2（npm view 版本列实证）。

**宿主不变量桶**：5 工具 mcp__anysearch__* 桥接可见可调（T0 boot4 端到端真搜索）；server-down fail-open（policy.test + dsh-plugin 单测覆盖）；无 bundle=纯 Phase-1 降档面文档化。

**宿主变量桶**（`t1-wire-requests-deny-distill-inject.log` + `t1-ans-ipc-calls.log`）：routing-card durable 注入 + systemPrompt section 入线（LLM stub 请求体 marker）；denylist URL（blocked.example）→ 工具调用 `requires approval`（URL 门 fail-closed 契约）；anysearch preheat 上下文入线；蒸馏摘要 + `POST /index` 真 IPC 带 `x-anysearch-session-id` + traceparent。

**宿主新增桶**：同 id 双 insert 硬错实证（`t2-dup-insert-loader-error.log`——`--dump-config` 容忍双行渲染，boot 激活期 `duplicate loader entry id: mcp-anysearch` 硬错）；`- id:` 打缺失行仅告警；web 组合体共享行撞名（code-runtime）实录+overlay 解法；churn lint 控件先红后绿（见下）。

**web 具名两探针**：web profile 组合体 `dsh web` `127.0.0.1:3080` + 驱动 turn——复跑 transcript `t2-web-rerun-turn.log`（`dsh web: http://127.0.0.1:3080/?token=...` + `PROBE_TURN_COMPLETE`）；ans-stub 四 IPC 全落 `t2-web-ans-ipc.log`（`GET /policy` ×2 + `POST /recall` + `POST /index` 带 `sess=session-b94b6a86…` 传播 + 10 条真搜索结果 entries）；LLM 线体 `t2-web-llm-wire.log` 五 marker（card+deny+distill+preheat）。交互式 web UI turn 标 untested（deferred 记档）。注：首跑 boot10 的 /recall+/index 未留 stub 记录（疑会话持久化注入复现），复跑独立实证全 IPC——证据以复跑为准。

**doctor 腿**：`node apps/cli/dist/index.js doctor` → `Result: 25 passed, 0 skipped, 0 failed`（doctor 无宿主检查=fail-open 姿态不受影响；本轮改动下绿）。

**churn lint 红绿对**：ship-gate step 1s 新控件——绿：干净树 --quick 全跑 `[pass] ADR-0073 dsh-plugin churn lint`；红：临时 commit 注入 `@deepseek-ai/dsh-tools` 入 dependencies → 真门 `[fail] ADR-0073 churn lint: @deepseek-ai/* in dependencies` → `but undo` 撤销恢复（演示 commit 未留史）。

**verified-hosts 第 6 行**：README.md + README.zh-CN.md 双语行（DeepSeek Harness 0.1.5-rc.2 / 2026-09-19 / 两阶段 scope / live-verified 实录 + 未测面标注）；双语 parity 门绿。

### (iv) 文书收口段

- ADR-0073 已入 docs/adr + index 73 条 regen 提交（`uyt`）。
- CONTEXT.md 新增两落地词块：Cordis Patch Loader Rules（loader 三律）+ ESM Bundle createRequire Shim。
- CHANGELOG.md 新段 `## 2026-09-19 — ADR-0073 r72`（Added/Fixed）。
- found/fixed/deferred：见下节逐条闭环。
- path-lint：`ship-gate --quick` `[pass] path-lint: 239 registered doc(s) clean`——本票文档（reports/handoffs/evidence 目已注册）全过；next-round.md Stack 粗体违规已修（`nnn`）。
- but commit 工作区干净：最终状态 `git status --porcelain` 空（RED-DEMO commit 已 undo）。

## found / fixed / deferred

**found→fixed（轮内闭环）**：
1. ESM bundle 混 CJS dep → `Dynamic require of "node:crypto" is not supported`（真 loader）→ fixed：esbuild banner 注入 createRequire（T1 boot5 transcript 实证）。
2. `- insert:` Phase-1 手写行与 bundle 行同 id → `duplicate loader entry id` 硬错 → fixed：用户层改 `- id:` 打靶整行复述（T2 boot8 实录+文档三律）。
3. web+headless 直拼 profile → code-runtime 同 id 撞 → fixed：web overlay 只插所需行（boot10 实录）。
4. next-round.md `**Stack**` 粗体不匹配 locatorLinePatterns → fixed：去粗体对齐惯例（nnn）。
5. tsconfig noEmit 压 declaration 构建 → fixed：build 脚本 --noEmit false --emitDeclarationOnly。
6. @deepseek-ai/dsh-llm 类型缺 devDep → fixed：补录 devDependencies。

**环境发现（非代码缺陷）**：
- policy.test 本地失败=我自持的 ans-stub 占 33333 端口污染（杀进程后 10/10 绿）——测试隔离教训，非回归。
- but status os error 1=早前 `2>nul` 误建 Windows 保留名文件 → 删 `nul` 恢复。

**deferred（docs/deferred-registry.json +3）**：
- defer-r72-dsh-plugin-npm-publish（发布道一字段翻牌）。
- defer-r72-dsh-native-tools（ctx.tools 原生注册，重返条件记档）。
- defer-r72-dsh-web-interactive-matrix（交互式 web UI + live reload + approval 面）。

## ship-gate 终态

`node scripts/ship-gate.mjs --quick` 全绿（含 step4 pack 全 8 包）：step0 三不变式 + step1 全部静态断言（含新 churn lint）+ ADR index + supersession + CONTEXT wiring + evidence-anchor + handoff-lint + readme-parity + path-lint 239 docs + step2 domains + step6/7 eval 126/126 + step8/9 MCP initialize + fail-open boot。step3 turbo check/test/build 手动跑通（check 8/8、test 全绿、build 全绿），step5 install 由 CI matrix 覆盖。

## 复跑指引

1. 单测：`pnpm -C apps/dsh-plugin test`（13/13）。
2. 打包：`pnpm -C apps/dsh-plugin pack --pack-destination <tmp>` → `dsh plugin --profile <p> add <tgz>` → `dsh --profile <p> --dump-config`。
3. 真宿主 turn：隔离 `$DSH_HOME` + ans-stub(:33333) + llm-stub(:28123 SSE) → `dsh --profile headless "<task>"` → `PROBE_TURN_COMPLETE` + 三 IPC 上线路。
4. 门：`node scripts/ship-gate.mjs --quick`。
