# Round-72 审计报告 — DeepSeek Harness（dsh）宿主适配

审计日期：2026-09-19。审计对象：`.scratch/grill-round-72/reports/2026-09-19-report.md` + `handoffs/round-72-closeout.md` + `reports/spike-report.md`（实施方自述）。
审计方法：不信自述，全部亲跑/亲验；硬验收逐项复现（含真宿主测活独立重跑）；双轴评审（Standards + Spec，两个并行只读子代理）；D-001~D-004 逐条对账。
评审基准点：`c4ced36a`（任务书 Stack base，merge-base 实测一致）→ `5494c91b`（r72-grill tip `zmu`）；diff 3,479 行 / 56 文件（r71-audit 平行栈不在链内，零污染）。
审计窗口职责：只出报告不动手修。

## 1. 硬验收（亲跑复现，非抄报告）

| 验收项 | 命令 | 报告声称 | 实测 | 结论 |
|---|---|---|---|---|
| 编译 | `pnpm turbo run check` | 8/8 | **8 successful, 8 total**（dsh-plugin tsc --noEmit 真跑非 cache） | ✅ |
| 单测 | `pnpm -C apps/dsh-plugin test` | 13/13 | **13 pass / 0 fail** | ✅ |
| 工作区测试 | `pnpm turbo run test` | 全工作区执行 | **13 tasks successful**（dsh-plugin 13/13、plugin 10/10、store/mcp/cli/retriever/embedding 全绿） | ✅ |
| 打包 | `pnpm -C apps/dsh-plugin pack` | tgz 五件 | `anysearch-cli-dsh-plugin-0.0.6.tgz` = `cordis.patch.yml + lib/index.js + lib/index.d.ts + LICENSE + package.json`，一件不多 | ✅ |
| ship-gate | `node scripts/ship-gate.mjs --quick` | 全绿 | **exit 0**：step4 全 8 包含 dsh-plugin、eval 126/126、MCP initialize、fail-open boot、path-lint/handoff-lint/readme-parity 全过 | ✅ |
| doctor | `node apps/cli/dist/index.js doctor` | 25/0/0 | **25 passed, 0 skipped, 0 failed** | ✅ |
| 真宿主测活 | 隔离 `$DSH_HOME` + 自起 fresh stubs + `npx -y -p @deepseek-ai/dsh@0.1.5-rc.2 dsh --profile headless "<task>"` | PROBE_TURN_COMPLETE 干净退出 | **PROBE_TURN_COMPLETE** + "anysearch MCP server: stdio transport ready"；模型真调 `mcp__anysearch__search_web`，10 条真搜索结果（tavily）落 `POST /index`；`GET /policy` + `POST /recall` + `POST /index` 三 IPC 全上线，`sess=session-cc16038b-…` 传播。transcript 归档 `evidence/audit-r72-*.log` | ✅ 独立实证 |
| 幂等安装 | `dsh plugin --profile headless add <同一 tgz>` | `Already up to date` no-op | **Already up to date**，pnpm 11.24.0（=pin）转发 | ✅ 独立实证 |
| dump-config 层 | `dsh --profile headless --dump-config` | bundle 层 + patched-by | `# == @anysearch-cli/dsh-plugin` 层 + `patched by …/cordis.patch.yml` 用户层标记 + `mcp-anysearch` 行在 | ✅ 独立实证 |
| 环境 pin | node/pnpm | v24.11.0 / 11.24.0 | **node v24.11.0 + pnpm 11.24.0** | ✅ |

## 2. 声明 → 证据 → 结论

| 声明 | 亲验证据 | 结论 |
|---|---|---|
| spike-report 实物：9 探针逐项 PASS/FAIL/RESHAPE + #8 blocking + Phase-2 GO | `reports/spike-report.md` 存在，verdicts 表 9 PASS / 0 FAIL / 0 RESHAPE，#8 config+runtime 双层判定；机制锚点（waterfall await、durable inject、section API、命名、plugin add 登记、整行替换）与后续实现一致 | ✅ |
| apps/dsh-plugin 包五件（src/patch/pkg/tsconfig/test/AGENTS.md） | 全部存在；`src/index.ts` 158 LOC、五面注册（session-start inject + systemPrompt section + pre-execute + post-execute + tools/result）、全走 `127.0.0.1:33333` IPC、每面 try/catch fail-open | ✅ |
| package.json：private/type:module/dsh.bundle.patch/零运行时依赖/devDep 类型耦合/keywords | `package.json` 逐字段核：private:true、type:module、`dsh.bundle.patch:"./cordis.patch.yml"`、**无 dependencies 键**、`@deepseek-ai/*`×5 仅 devDependencies、keywords 含 `dsh-plugin`、files=[lib,cordis.patch.yml] | ✅（F6 注记 files 未含 AGENTS.md） |
| cordis.patch.yml：insert 插件行 + mcp-anysearch 桥行整行复述 | 两行 `- insert:`：`anysearch-dsh-plugin` + `mcp-anysearch`（serverName/transport/command/args/toolCallTimeoutMs/failOnStartupError/reconnect 全 key 复述） | ✅ |
| Phase-1 配置行文档实证 + loader 三律 | `docs/deepseek-harness-integration.md` §Phase-1 行全文 + 三律记档（insert 加行 / `-id:` 打缺失仅告警 / config 整行替换 + 同 id 双 insert 硬错） | ✅ |
| apps/plugin 窄口子：exports + esbuild entries + dist 产物入 pack | `apps/plugin/package.json` exports 含 `./hooks/routing-card`、`./hooks/server-token`；build:hooks esbuild 入口列同两文件；`dist/hooks/routing-card.cjs`+`server-token.cjs` 存在且在 plugin tarball 内 | ✅ |
| 装拆重装幂等（dsh.profile.bundles + file: dep 双字段登记/清理） | profile `package.json` 实证：`bundles` 含 `@anysearch-cli/dsh-plugin` + `dependencies` 记 `file:…tgz`；亲跑同 tarball re-add = Already up to date；`t2-idempotent-install.log` 含 remove/re-add 序列 | ✅ |
| 宿主不变量：5 工具桥接可见可调 | 亲跑 wire：`mcp__anysearch__{search_web,research_web,recall_memory,query_knowledge,ans_chat}` 全 5 工具在 tools[]；模型实调 search_web 端到端 10 条回；`ANS_TOOL_PATTERN`（core.ts:41）regex 亲验匹配 `mcp__anysearch__*` 形态 | ✅ |
| server-down fail-open 双臂 | `t2-headless-serverdown.log` + `t2-bundle-serverdown.log` 存在；单测 "server down fails open to allow" 绿；URL 门 fail-closed 契约（policy 不可得→ask）在 preheat.ts:78-82 | ✅ |
| 宿主变量：routing-card durable + section 入线 + deny + preheat + 蒸馏 + /index session 传播 | 亲跑 wire/stub 全上线：system prompt 含 `[anysearch plugin active]` 卡片段、`blocked.example` 走 deny→error→工具结果回路、`[anysearch preheat]`/`R72 prior probe` 入线、`POST /index` 带 `sess=session-…` + 10 entries | ✅（traceparent 见 F5） |
| 宿主新增：同 id 双 insert 硬错 | `t2-dup-insert-loader-error.log` 含 `duplicate loader entry id: mcp-anysearch`（dump-config 容忍双行渲染、boot 激活期硬错） | ✅ |
| web 具名两探针：`dsh web` :3080 + 驱动 turn + 全 IPC | `t2-web-rerun-turn.log` 含 `127.0.0.1:3080` + `PROBE_TURN_COMPLETE`；`t2-web-ans-ipc.log` policy×2+recall+index `sess=session-b94b6a86-…`；`t2-web-llm-wire.log` 卡片+deny+preheat 标记在 wire；`webprobe` profile 实物=web-app+plugin bundle + headless 作 plain dep + overlay 只插两行（code-runtime 撞名解法如实） | ✅ |
| Phase-1-only 降档面对照 | `t2-phase1-only-boot.log` 存在 | ✅ |
| tarball profile turn | `t2-tarball-profile-turn.log` 存在 | ✅ |
| 升级 diff 演练（rc.2→0.1.6-alpha.2 tsc 绿→回钉） | 报告+ADR-0073 叙述在（"pin restored to the verified train"），**evidence/ 无 transcript 实物** | ⚠️ 叙述级（F4） |
| doctor 腿 fail-open | 亲跑 25/0/0；dsh 侧事实由 dump-config/boot transcript 覆盖（解释性满足） | ✅（口径注记 F4） |
| churn lint 控件（step 1s） | `scripts/ship-gate.mjs:452-477` 实物：dependencies/peerDependencies/optionalDependencies 三字段 `@deepseek-ai/*` 泄漏 fail-closed + private/type/dsh.bundle.patch/patch tokens 断言；绿态亲跑 `[pass]`；红态为临时 commit+undo 演示未留 transcript（见 §5-P1） | ✅（红腿欠存档） |
| 新包入 ship-gate/构建/测试清单 | `ship-gate.mjs:51` `apps/dsh-plugin` 在 PKG_DIRS；turbo check/test 8 包含之（亲跑） | ✅ |
| verified-hosts 第 6 行双语 | `README.md`+`README.zh-CN.md` 均含 DeepSeek Harness / 0.1.5-rc.2 / 2026-09-19 / scope / verified + untested 标注 | ✅ |
| ADR-0073 入账 + index 73 | 文件在（D2 churn alarm、D4 三桶分类法、native-tools 演进条件、watch items）；`docs/adr/` 73 个编号文件 | ✅ |
| CONTEXT 两词块 | Cordis Patch Loader Rules + ESM Bundle createRequire Shim 块在（R72 词族共 11 处命中） | ✅ |
| CHANGELOG r72 段 | `## 2026-09-19 — ADR-0073 r72` 在 | ✅ |
| deferred +3 | registry 含 `defer-r72-dsh-plugin-npm-publish`/`dsh-native-tools`/`dsh-web-interactive-matrix`；r71 三项（shipgate-1g/transformers/provider-serverside）续账在 | ✅ |
| found 6→fixed 6 | 逐条对物：createRequire banner（package.json:39 + lib/index.js 实物）、duplicate-insert（transcript）、code-runtime overlay（webprobe patch 实物+注释）、Stack 粗体（nnn commit+现文无粗体）、noEmit→emitDeclarationOnly（build script）、dsh-llm devDep（package.json:48） | ✅ |
| but commit 工作区干净 | `git status --porcelain` 空（审计前）；r72-grill 7 commit 链清晰、与 r71-audit 平行零交叠 | ✅ |
| 范围外零触碰（ctx.tools 原生注册/npm 发布/web 全矩阵/R71 遗留） | diff 56 文件全属轮内面；无 npm publish、无 ctx.tools 注册、web 仅两探针；R71 deferred 三项续账未被拉动 | ✅ |

## 3. D-001 ~ D-004 对账

- **D-001（两阶段 C 定界）**：Phase-1=文档化 MCP 桥行（integration.md §Phase-1）✓；Phase-2=薄 bundle 只做 hooks 层五面、业务逻辑全进程外 IPC（src/index.ts 无任何检索/密钥/DB 逻辑）✓；spike 门控走完且 #8 blocking 亲验通过、GO 合规 ✓；B/A 否决记档于账本+ADR；原生注册 deferred+ADR 演进条件 ✓；R71 遗留续 deferred 未拉动 ✓。**落地。**
- **D-002（包形态）**：零运行时依赖 private 新包 ✓；dsh.bundle patch 整行复述 ✓；类型仅 devDeps=churn alarm + step-1s 门控三 dep 字段 ✓；tarball→add→dump-config 一步装亲跑实证 ✓；预发布 lint 实现但 `name`/`files` 未断言（F2）。**落地，附 F2。**
- **D-003（票序）**：T0 九探针 #8 blocking PASS→T1 双件（单测绿+Phase-1 行）→T2 三桶全量+web 两探针+幂等+doctor+churn lint+tarball+verified 行+integration doc+门注册→T3 文书收口，顺序合规；web 腿只具名两探针余标 untested 如实；HMR 检查部分 deferred（F4 显式）。**落地，附 F4。**
- **D-004（收口判据）**：四段全齐——(i) spike 报告实物+transcript；(ii) 包 diff+单测绿+Phase-1 实证；(iii) 三桶全量证据+web 两探针+幂等+doctor+churn lint+verified 行；(iv) ADR+CONTEXT+CHANGELOG+found/fixed/deferred+path-lint 自证（239 docs clean）+工作区干净。**落地。**

## 4. 双轴评审（并行子代理，独立收敛）

### Standards
- **文档标准违例 2 项（皆小）**：closeout Stack 行只列 but-id 无 SHA+捕获日期、末条"收口文档提交"无 but-id（template 要 `<but-id> (<sha> @ <iso-date>)`）；`dependencies` 键缺省而非字面 `{}`（AGENTS.md 不变量字面义，语义等价、门通过）。
- 判定项：index.ts 118-125/141-148 HookInput 构造重复（可抽 helper）；注释 "Four hook responsibilities" 实为五条目（测试同称 four）；`await next()` 在 try 外（downstream 抛错上抛宿主——但 Cordis 容器化 listener 失败，属可辩护取舍）；tsconfig `include:src/**` 使 test/ 不参与 tsc；turbo `outputs:dist/**` 对不上 `lib/**`（build 永不命中缓存）；`files` 未含 AGENTS.md（apps/plugin 含之）；ship-gate "1s" 插序 + "1n" 重号（pre-existing）。
- 已核合规：五面 fail-open、URL fail-closed 走共享 `makePreToolUseDecision`、patch 整行复述、hooks 逻辑复用非重实现、ADR-0072 三分教义、双语 parity、index/registry 一致、gitignore 正确。

### Spec
- (a) 缺失/部分：HMR 检查降为显式 deferred（headless=startup 实证、web=live 未验）；预发布 lint 缺 `name`/`files` 断言；升级 diff 演练叙述级无 transcript；doctor 腿为仓内 ans doctor（dsh 侧由其他 transcript 解释性覆盖）。
- (b) 未请行为：无实质 scope creep；plugin exports 窄口子为复用 mandate 必需。
- (c) 实现但疑错：preheat recall 腿在 waterfall 内 `await`（非字面 fire-and-forget，死服器最坏 ~8s/call，与"不重实现"不变量一致、/index 腿确为 fire-and-forget——判定项 F3）；integration.md "Layering rules (verified…)" 下 web `patchReload:"live"`/listeners dispose 无 transcript（F1）；integration.md `dsh plugin add @anysearch-cli/dsh-plugin` 命令在 private 态不可跑（F1 同域）；票文 `agent/created` vs 实现 `agent/session-start`（spike 正名、票文未回改，cosmetic）。

## 5. 发现清单

- **F1（文档过声称，小）**：`docs/deepseek-harness-integration.md:91-93` "Layering rules (verified on the real loader)" 断言行——`patchReload:"live"` 下 "listeners are fiber-scoped Cordis effects and dispose cleanly on reload" 无 live-reload transcript（交互矩阵已 deferred）；同文件 :58 `dsh plugin add @anysearch-cli/dsh-plugin` 在 private 态不可执行（仅 tarball 路径验过）。建议：措辞降档为"expected/unverified"或标注 untested；安装命令改 tarball 形或注明发布后生效。
- **F2（门缺口，小）**：D-002 预发布 lint 要求断言 `name/dsh.bundle/files/type:module`；step-1s 断言了 type/private/dsh.bundle.patch/patch tokens，未断言 `name`、`files`。建议补两断言行。
- **F3（判定项，可辩护）**：`src/index.ts:86-106` pre-execute 内 `await makePreToolUseDecision` 串行等待 /policy（3s）+/recall（~5s）后才 `next()`——票文 "preheat fire-and-forget" 字面未守；但 URL deny 门本就必须阻塞（fail-closed 语义），recall 腿阻塞换来"本次调用完成前上下文已落 durable"的确定性，且与"共享实现不重写"不变量一致。维持现状可接受；若从严可将 recall 注为非阻塞 inject。
- **F4（证据薄，小）**：升级 diff 演练与 dsh-doctor 腿为叙述级（无 transcript 实物）；HMR 检查部分 deferred 属显式合规但与 F1 的"verified"措辞冲突。
- **F5（证据缺口，小）**：`traceparent` 无 transcript 存档——ans-stub 只记 auth+sess 两 header；机制代码实证在 `apps/plugin/src/hooks/propagation.ts`（callServer 注入 traceparent+x-anysearch-session-id），session 传播亲跑上线。建议下轮 stub 记录全 header 即闭环。
- **F6（cosmetic 组）**：注释 "Four hook responsibilities" vs 五条目；dependencies 键缺省 vs "{}" 字面；closeout Stack 行缺 SHA+date 及 zmu but-id（报告 Stack 行同漏 zmu）；tsconfig 不覆盖 test/ 类型检查；turbo `outputs:dist/**` vs `lib/**`（dsh-plugin build 不可缓存）；index.ts HookInput 构造重复；`files` 未含 AGENTS.md；ship-gate 步骤标号插序/重号（pre-existing）。

## 6. 过程呈报（不追认，只列事实）

- **P1**：churn lint 红绿对之红腿为临时 commit+`but undo` 演示，未留 transcript 存档（报告已如实声明"演示 commit 未留史"）；控件代码层 fail-closed 可核（ship-gate.mjs:463-467）。
- **P2**：审计开始时 28123/33333 端口被实施窗口遗留 stub 进程占用（`llm-stub.mjs`/`ans-stub.mjs`，与其自记"端口污染"教训同源）——已 kill 并以审计自控实例重跑，transcript 另存 `evidence/audit-r72-*.log`。
- **P3**：报告 Stack 行列至 `nnn` 止、漏 `zmu`（收口 commit 本身，自指缺漏）；closeout 已以"收口文档提交"文字承认该提交存在。
- **P4**：Standards 子代理环境无 shell 未能执行 `git diff`，按后-diff 树评审；diff 范围由本窗口独立 `git diff --stat`（56 文件）补足。
- **P5**：报告如实披露首跑 boot10 /recall+/index 未留 stub 记录、以复跑 transcript 为准——验证诚实度合格，追认此处置。
- **P6**：提交链线性（d72b1e97→5494c91b），无 force-push 迹象；`r72-grill` 与 `r71-audit` 平行互不写，工作区审计前干净。
- **P7**：审计件（本报告+交接+transcript）将落 `r72-audit` 新分支，与实施栈平行互不写（GitButler 协议）。

## 7. 裁决

**通过（PASS，附次级发现）**。报告关键声明全部经亲跑/独立实证成立：硬验收 10/10 复现（含真宿主 PROBE_TURN_COMPLETE+真搜索+三 IPC+幂等+层核对的独立重跑）；D-001~D-004 全落地；范围外零触碰。F1-F6 均为次级项（文档措辞/门覆盖/cosmetic），无一构成返工级缺陷。

处置建议（呈报给用户裁决，审计窗不动手）：
- 选项 A：F1-F6 打包为下轮 due-chore（建议优先 F1 措辞降档 + F2 两断言行 + F6 cosmetic 组）。
- 选项 B：打回实施窗返工（按本仓惯例次级发现可同票修，修后重跑 §1 同套验收）。

## 8. 修复轮（用户裁决：立即修复——子代理执行，审计窗独立复核）

用户裁决取"直接修复 + LOOP 复验"。修复经子代理实施，审计窗逐 diff 复核；修复件落 `r72-audit`（因修复对象多为实施栈新建文件，`r72-audit` 已堆叠至 `r72-grill` 之上）。**同套验收重跑：turbo check 8/8、dsh-plugin test 13/13、pack 六件（+AGENTS.md）、doctor 25/0/0——全绿；ship-gate --quick 于提交后清树重跑见 §8 尾注。**

| 发现 | 处置 | 实物证据 |
|---|---|---|
| F1 | 安装命令改 tarball 形 + post-publish 注记；`patchReload:"live"` 标 EXPECTED/unverified（引 defer-r72-dsh-web-interactive-matrix） | `docs/deepseek-harness-integration.md:57-61,93-97` |
| F2 | step-1s 补 `name === @anysearch-cli/dsh-plugin` + `files ⊇ {lib, cordis.patch.yml}` 两断言 | `scripts/ship-gate.mjs:472-475` |
| F3 | 判定项维持现状；surface-3 注释显式记录 recall 腿有意阻塞（deny 门 fail-closed 必须阻塞 + preheat 先于门控调用落 durable；IPC 超时上界） | `apps/dsh-plugin/src/index.ts:91` |
| F4 | 升级 diff 演练实跑留 transcript；dsh-doctor 腿补齐（`dsh --version` + dump-config 层核对） | `evidence/audit-r72-upgrade-diff.log`、`evidence/audit-r72-doctor-leg.log` |
| F5 | stub 记全 header，实跑捕获 `traceparent` 上线（/recall + /index 两笔，W3C `00-<trace>-<span>-01` 形）；归档 stub 同步记 tp/ts | `evidence/audit-r72-traceparent.log`、`evidence/ans-stub.mjs` |
| F6 | 全组落地：注释/test 名改 five hook surfaces；`"dependencies": {}` 字面化；closeout/report Stack 行补 but-id+SHA+date 及 `zmu`；tsconfig include 覆盖 test/（新增 `tsconfig.build.json` 保 emit 边界，build 用 `-p` + `--rootDir src`）；turbo `outputs` 增 `lib/**`；`postHookInput()` 提取去重；`files` +`AGENTS.md`（tgz 五件→六件）；ship-gate 标号乱序为 pre-existing 惯例（1i/1j/1n/1o 均重号），保持原状 | 各文件 diff |
| **F7（演练新发现，deferred）** | **上游 0.1.6-alpha.1 已把 `agent/session-start` 移出 `keyof Events`（TSC_ALPHA1=RED @ src/index.ts:87）——churn 警报按 ADR-0073 设计真实触发；且 `^0.1.5-rc.2` caret 在传递依赖重解析时漂至 alpha.1 → 混版树 `MessageId` brand 冲突（TSC_REPIN=RED）。committed lockfile（全 rc.2）保 fresh install 绿；0.1.6-alpha.2 被 `minimumReleaseAge: 2880` 拦截（门禁按设计生效，未绕过）。建议下轮：dsh-* devDeps 精确钉版或 `pnpm.overrides` 锁传递 dsh-*，并评估 `agent/session-start` → 新事件名迁移** | `evidence/audit-r72-upgrade-diff.log` post-mortem |

### 最终裁决（修复轮后）

**通过（PASS）**。F1-F6 全部修复并经审计窗独立复验（diff 逐项核对 + 同套验收重跑全绿）；F7 为修复轮演练新暴露的上游演进风险，当前 committed lockfile 下无即时影响，记录为下轮 follow-up。本轮交付维持"功能真实、验收真绿"结论，证据链较修复前更完整（traceparent/upgrade-diff/doctor 腿均已有 transcript 实物）。
