# Round-60 Fixer 实施报告 — ADR-0061 垂直域交付闭环

日期：2026-09-14 ｜ 执行者：修复子 Agent ｜ 覆盖 D-001..D-006 全账本
分支栈（自底向上）：grill-60-docs → t1(xzr) → t2(lqu) → t3(mpt) → t4(wym) → t5(otx+msv) → t6(rpq)

## 逐票结果

### T1/B1 docs 域 walking skeleton — ✅ 交付

- `domains/docs.toml`：sources=[tavily,exa,anysearch]，urlAllowlist=[modelcontextprotocol.io, typescriptlang.org, www.typescriptlang.org, pnpm.io]，skills=[search,doctor]，rag.adapter=none，hooks 白名单 3 项。
- `eval-looks.json` 顶层 `golden` 集（schema `anysearch/docs-golden@1`）：11 条全部 provenance 可回访（8 internal-dogfood + 3 external-community：SO 79923194 / python-sdk#2278 / pnpm#12749 逐字），零合成。
- `eval-looks.coverage.json`（`anysearch/eval-looks-coverage@1`）：8 维，7 covered + adversarial 显式 deferred（触发点挂 B4 后续轮）。
- 域解析链：ANS_DOMAINS_DIR → cwd/domains → 包内 domains → repo-root；`ans domain <name>` 持久化到 ~/.anysearch/config.env（0600），入口 rehydrate 不覆盖显式 env（含空串）。 <!-- machine-local: 用户级 ~ 路径引用（存量合规化） @ 2026-09-22 -->
- looks-ledger 修 true bug：appendLook/压实路径不再静默丢 `golden` 顶层集。
- 证据：`node scripts/validate-domains.mjs` 3/3 OK；`eval-docs-golden.test.ts` 35/35；安装物 doctor 19/0/0。

### T2/B2 装到用链路 CI — ✅ 交付

- `scripts/install-smoke.mjs`：7 包 pack → 干净 npm 前缀装 → 安装物 ans --version/doctor/domain docs/search 实跑；拒绝 0.0.0 陈旧包；online（有 provider key）硬断言 ≥1 结果命中 allowlist 主机，offline 断言文档化行为。
- CI：.github/workflows/ci.yml 加 install-smoke job（ubuntu+windows）；macOS limitation 已在工作流注释记录（better-sqlite3 arm64 prebuild）。
- 实跑证据：`install-smoke: 15 passed, 0 failed`（含 online URL 硬断言真命中 modelcontextprotocol.io）。
- 顺带修复两个真实 bug：shebang tsx→node（tsx 非运行时依赖）；CJS bundle 下 import.meta.url 为 undefined 的 db.ts 双模自定位。

### T3/B4 badcase→golden 回灌闭环 — ✅ 交付

- `eval-badcases.json`（`anysearch/eval-badcases@1`）+ `scripts/badcase-backfill.mjs`：`--into` attach（同问句回指既有 golden 作回归证据）/ `--new` 建新条目并自动重算 coverage manifest；无 observed+evidence.command 拒绝回灌（禁合成护栏）。
- 真实种子 bc0001：安装物实跑中 tokio 域外提问期望 abstain 实返 10 条非 allowlist 结果 → 已 attach 到 docs-g0007（同问句），notes 记入观测。
- 回归捕获：eval-docs-golden.test.ts 第 9 段（badcase schema/promotedTo 可解/逐字问句/open 态防重复回灌），CI 每跑必检。
- 证据：`node scripts/badcase-backfill.mjs --id docs-bc0001 --into docs-g0007` → attached；测试 35/35。

### T4/B3 doctor 自服务 — ✅ 交付

逐条点名 T1/T2 实测暴露缺口：
- [4] 新增 ANS_DOMAINS_DIR 可见化 + durable DB 路径 mkdir/可写探测（此前 DB 失败只在 search 时炸）。
- [5] 新增活动域 sources.enabled 逐 provider key 覆盖——未设 key 记 SKIP 并给双向修复（设 key 或移出 enabled）；域不在解析链/链上无 TOML 点名修复命令。
- `ans domain` 失败列可用域 + ANS_DOMAINS_DIR 指引，明示 silent full-fanout 语义。
- clig.dev：失败摘要走 stderr，exit 0/1 契约不变。
- 证据：`node dist/index.js doctor` → 22 passed, 2 skipped, 0 failed, exit 0；e2e 11/11；install-smoke 15/15 回归仍绿。

### T5/C1 release-lines 对真实 pack 实跑 — ✅ 交付

- 版本升至 `0.0.1`（D-006 patch 起步，诚实信号）：7 个 workspace 包 + 根 package.json；ship-gate 陈旧 0.1.0-rc.0 字符串清理。
- gitignore-drift 不变量修复：`.scratch/*` 子项排除 + round-60 治理产物显式纳回（D5c），pack 产物 t1-pack 保持忽略。
- `node scripts/ship-gate.mjs --skip-matrix` 全门禁绿：9 步含 7 包 @0.0.1 真实 pack（tgz 含 domains/*.toml）、T0 smoke、eval gate 126/126、MCP initialize v0.0.1、fail-open boot。
- release-gate 说明：--pre-tag 决策级 OF look 仅 release.yml 在 tag 流中花费（OF 预算纪律）；本地不空转实跑。

### T6/G1 graceWindow/deepMode — ✅ 裁决：implement

- 裁决依据（B1-B4 证据）：本轮真实搜索路径全程走 engine.search 而未触发早停；per-provider AbortController 已就位但永不 fire；providersCancelled 元数据字段存在却恒空——是「留了 API 面没留行为」的真债。
- 实现：pool 覆盖 `q.maxResults` 唯一 URL 后 straggler 获 graceWindowMs 再 abort；deepMode 恒等全部；rejected 计为已到达。
- 哨兵翻正：ship-gate step1c / t6-hostile-cuts / CONTEXT.md / ponytail-debt-ledger 全部从债注态翻为接线态。
- 证据：engine.test.ts 67/67（新增 5 断言：取消上报+abort 实发+deepMode 等齐+未达阈值全等）；turbo check/test 17/17。

## 验收总闸（本报告写后复跑，见末尾）

计划：turbo check / build / test --force / gen-adr-index --check / check-workflows / ship-gate --skip-matrix。
