# Round-64 Implementation Report — 2026-09-16

Scope: `.scratch/grill-round-64/handoffs/next-round.md` T1–T6, D-001..D-007.
Branch: `r64-impl` (GitButler), commits `rru` `syq` `rzs` `pzn` `nsy` `zun` (+T6/report commits below).

## T1 — mechanism layer (commit rru)

- Runner quarantine classification unified onto ledger `activeIds()` (was an inline copy ignoring `longterm`). Evidence: `pnpm -C packages/store check` clean; `eval-looks-live-parity.test.ts` 22/22 (mixed longterm/expired/retired ledger asserts `activeIds()==isActive()` per entry + no-inline-copy source assertion).
- Evidence mode `ANS_EVAL_EVIDENCE=1`: quarantined entries execute (`EVIDENCE-RUN` lines), `EVIDENCE <id> <pass|fail> <ISO-ts> <run-ref> p=<n> f=<n>` appended to `ANS_EVAL_EVIDENCE_LOG`, only-quarantined-failures → exit 0 (`EVIDENCE-ONLY-FAILURES`), consecutive all-red → `RETIRE_CANDIDATE`. Verified live: run-01 (pre-migration, old assertions) → 10 quarantined entries all executed, 4 pass / 6 fail, exit overridden to 0; `.scratch/grill-round-64/evidence/run-01-pre-migration.out`.
- `mustHitPaths`/`mustNotHitPaths` matchers: pathname substring, host-scoped to `mustHitHosts` when declared; `urlHit()` untouched.
- Schema additive fields `mustHitPaths`/`mustNotHitPaths`/`stability_class`/`failure_class`/`migration`/`watch` + root `schema_version: 1` in `packages/store/src/eval/docs-golden.ts` + `eval-looks.json`.

## T2 — fixture migration (commit syq)

- 8 path-drift entries (g0001/2/3/4/5/6/9/11) migrated to `mustHitPaths` with `mustNotHitPaths` negative pins; fragments calibrated on `probe-0.json` live returns (not guessed).
- g0008 downgraded `mustHitHosts` → `[pnpm.io]` (`failure_class: locale-clustering-suppressed-cross-host`; `hops:multi` kept as dimensions observation).
- g0010 took D-003 fallback: live answers resumed → merged into `mustHitPaths` cluster (`/deprecated` family), no abstain re-spec.
- 3 byte-exact legs retained, all `stability_class: frozen-spec` on dated MCP spec snapshots, recurrence-verified: g0001→2026-07-28 (3/3 probe-1), g0005→2025-11-25 (6/6), g0010→2024-11-05 (3/3). g0001's initial 2025-06-18 leg rotated out runs 03–06 → re-anchored (recorded in its `migration.drift`).
- Every migrated/downgraded entry carries `migration` {from,to,drift,decidedAt} + `stability_class` + `failure_class`.

## T3 — evidence + adjudication (commit rzs)

- Evidence artifacts: `.scratch/grill-round-64/evidence/` — `probe.mjs`, `probe-0.json/out`, `run-01..09.out`, `evidence.log`, `adjudicate.mjs`, `adjudication.json`, `promote.mjs`.
- Post-migration verdicts (runs 02–09): g0002/3/4/6/8/9/10/11 8/8 green; g0005 7/8 (one transient abstain run-07); g0001 3/3 post leg re-anchor. Flaky three flip=0 across 8 runs.
- `promoteEntry` ×10 via ledger API (`promote.mjs`), review notes cite evidence; ledger now `entries: []` — ratchet view empty. `watch:true` on g0001/4/5/6/9/10 (g0010 armed post-audit F4).
- `RETIRE_CANDIDATE` never fired (no consecutive all-red) — consistent with all-green adjudication.

## T4 — ship-gate anchors (commit pzn)

- `scripts/ship-gate.mjs` §1n now fails when: live `mustHitUrls` holders < 2; a byte-exact leg is anchored on non-`controlled|frozen-spec`; no live `mustHitPaths` holder; a migrated case lacks a complete `migration` block.
- Watch re-entry documented (ADR-0065 D5 + runner `WATCH` line): CI flip → re-quarantine via existing TTL path, same baseline id legal, renewals stay 0.

## T5/T6 — docs + closure (commits nsy, zun + this round's T6 commit)

- `docs/adr/0065-...` records D-001..D-007 + four obligations (ratchet boundary, re-spec sufficiency, longterm threshold, tombstone boundary, watch circuit) + closure evidence table backfilled.
- README Known Limitations bullet rewritten to page-family granularity; CHANGELOG entry added; ADR index regenerated (65 ADRs).

## Verification (rerunnable)

| claim | command | result |
|---|---|---|
| 编译通过 | `pnpm -C packages/store check` | tsc --noEmit clean |
| 全量离线测试 | `pnpm -C packages/store test` | 62 test files, 0 fail (incl. parity 22/22, docs-golden 44/44, quarantine 24/24, ratchet 12/12) |
| 打包通过 | `node scripts/ship-gate.mjs` | 9/9 steps pass; all tarballs packed, install smoke + dual-install ok |
| 启动并测活 | same (steps 8/9, 8b, 9) | MCP initialize green (server=anysearch v0.0.3), observation round-trip green, fail-open boot green |
| 线上 live 测试 | `pnpm -C packages/store test:online` | eval-looks-live 69/0 (12 entries, WATCH lines emitted), eval-semantic 4/0 |
| 证据模式验收锚 | `ANS_EVAL_EVIDENCE=1 node --import tsx test/online/eval-looks-live.online.ts` (from packages/store) | quarantined entries execute + EVIDENCE lines; only-quarantined failures → exit 0 |
| 棘轮终态 | `cat packages/store/eval-quarantine.json` | `entries: []`; ratchet subset/renewal/ruling checks vacuous-green |
| 平台闭环 | ship-gate on Windows (this box); CI ubuntu+windows matrix on push | local Windows green; CI URL PENDING — stack unpushed |

## Honest gaps

- All evidence is resolvable self-witness (own pipeline, replayable) — not external attestation.
- Byte-exact legs depend on provider coverage of frozen snapshots; coverage rotates (observed mid-round) — `watch:true` + TTL re-entry is the designed carrier for that risk, not fixture certainty.
- `test:online` lane is not part of ship-gate (observational per D-006 design); its local green above is the closest pre-push corroboration.
- No npm release, no OIDC work — deferred per scope.

## Post-audit rework (2026-09-16 audit window)

Audit `reports/2026-09-16-audit.md` verdict PASS (conditional), 4 findings fixed this window:

| finding | fix | re-run |
|---|---|---|
| F1 cosmetic — ADR gate leg marked done while CI URL pending | leg annotated done (local); CI URL pending push | doc-only |
| F2 honesty — probe.mjs stale sentinel comment + undocumented preference traversal | comment corrected; ADR-0065 D2 records sentinel-first had no attachable target so frozen-spec fallback taken directly | doc-only |
| F3 logic — RETIRE_CANDIDATE looser than 连续全红 spec + ungated on isQuar | lastEvidenceVerdicts carries p/f; gate requires prior allRed AND isQuar; parity test pins it | parity re-run |
| F4 fixture — g0010 frozen leg unwatched, flipped in audit window | watch:true set; promote.mjs/ADR/report/handoff records updated | test:online observation |

Post-rework re-runs: `pnpm -C packages/store check` clean; parity test pass; `node scripts/ship-gate.mjs` 9/9 incl. §1n anchors; `pnpm -C packages/store test:online` re-observed; eval-quarantine.json still entries:[].
