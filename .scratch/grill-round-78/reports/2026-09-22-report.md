# Grill Round 78 收口报告 — 治理工具链校准（repin 拦截实测 + pathlint 组合判定器）

Date: 2026-09-22。Stack：`r78-grill`(kok) ← `r78-impl`(nkv→zun→ryr→qqy→ovm→zrz→收口批)。任务书 `.scratch/grill-round-78/handoffs/next-round.md`，账本 `decision-ledger.md`（D-001~D-005 + D-006 实测落档）。

## 总判定

**三票全完、三段收口达成，含一格证伪如实呈报**。T0 七格判决矩阵实跑（E6 证伪——lockfile 回放不执法龄期闸，调研所载 #11583 在 11.24.0 不生效）；T1 pathlint 重构一票三 commit 全绿；T2 文书收口+门禁全绿+`but` 工作树零脏。

## T0 — repin 拦截实验（D-002，纯取证零提交）

预登记矩阵逐格实录，transcript `evidence/t0-repin-matrix.md`（含每格命令/输出/退出码）：

| 格 | 预期 | 实录 | 判 |
|---|---|---|---|
| E1 scratch 成熟版 install | 成功 | `Done in 2.1s`，+17 包 | ✅ |
| E2 scratch catalog→闸内版 | `ERR_PNPM_NO_MATURE_MATCHING_VERSION` | 同错误码，exit 1 | ✅ |
| E3 scratch + strict:false | 成功（显式旁路） | 成功+自动追加 `minimumReleaseAgeExclude` | ✅ |
| E4 真身 catalog repin 非 frozen | `ERR_PNPM_NO_MATURE_MATCHING_VERSION` | 同错误码，exit 1 | ✅ |
| E5 真身 + `--frozen-lockfile` | lockfile-out-of-sync | `ERR_PNPM_LOCKFILE_CONFIG_MISMATCH` exit 1 | ✅ 语义一致（执法点=config 一致性层） |
| E6 lockfile 携闸内版 + frozen | `ERR_PNPM_MINIMUM_RELEASE_AGE_VIOLATION` | **五变体（温/冷 store/显式 strict:true/`pnpm fetch`）全 exit 0 静默放行** | ❌ **证伪** |
| E7 无 time 字段源 | 装成功=向量 | npmjs 恒带 time 无标本 → 配置面向量记档不跑 | ◻ 记档 |

- 证伪处置（D-005 合法路径）：断言收窄为「闸只拦新鲜解析路径；lockfile 回放不执法」——落锚 ADR-0079 D3 + upgrade-ledger v2 待校准项消解 + 账本 D-006。
- 真身零脏恢复：复跑 `git show HEAD:pnpm-workspace.yaml | sha256sum` 前缀 `c68dfcff…`、`pnpm-lock.yaml` 前缀 `ce7fdbe9…`（实验前快照同值）；末态 `pnpm install` 绿。
- 标本龄期证据：`npm view @deepseek-ai/dsh-agent time` → `0.1.7-alpha.1` 发布 2026-09-22T06:04Z（实验时龄 ~481min < 2880min 闸内）。

## T1 — pathlint 重构（D-003 A++，一票三 commit）

### 判定器

`scripts/ship-gate-pathlint-detect.mjs`（新模块，161 行）+ `ship-gate.mjs` step 1i 接线替换 PATH_RE 块。规则=「token 检出+后随分隔符=locator」；四类新 token（`%VAR%`/`$VAR`|`${VAR}`/`~/`/`~\`/`\\host\`）+既有字面形；`/x` 单段根 info surfaced-skip；失效标记棘轮腿；inline code 不豁免。 <!-- machine-local: 判定器 token 形态示例引用（报告文书） @ 2026-09-22 -->

### warn-first → fail-closed 迁移实录

- 复跑：`node .scratch/grill-round-78/evidence/t1-warn-sweep.driver.mjs` → 313 文档 96 违例（93 missing-marker/2 stale/1 malformed）+685 info，清单 `t1-warn-sweep.md`。
- 处置：`node .scratch/grill-round-78/evidence/t1-remediate.driver.mjs` → 97 动作/52 文件（行尾补治理 marker 93+1、棘轮剥除失效 marker 3 处——含 driver 输出文档自证一例），实录 `t1-remediate.md`，复扫残存 **0**。
- 红绿 fixture：`packages/store/fixtures/pathlint/{red,green}.md` + `test/ship-gate-pathlint.test.mjs` 9 断言全绿（`node --import tsx --test test/ship-gate-pathlint.test.mjs` → 9 pass 0 fail）：两逃逸实例红向固化、每 token 类红绿成对、散文/URL/标记行/已标 fence 绿向全放、棘轮腿注入实测（stale-marker kind 断言）、inline code 不豁免实测、in-repo 命中 kind 断言。
- 直跑证据：ship-gate 1i 腿输出 `[pass] path-lint: 317 registered doc(s) clean` + 754 条 surfaced-skip info（见 ship-gate 运行存档）。

### Commit 结构（r78-impl 栈）

`nkv` fix（存量清理）→ `zun` refactor（判定器+fixture+测试）→ `ryr` chore（`minimumReleaseAgeIgnoreMissingTime:false`，`pnpm install` 绿实证：lockfile supply-chain policy 491 条核验过）。排序理由：fix 先行使每 commit 在各自判定器代际下皆绿（可二分）。

## T2 — 文书收口

- **ADR-0079** 落档+index regen 79 条（`node scripts/gen-adr-index.mjs --check` 在 ship-gate 1b 绿）。
- **registry**：`defer-r77-pathlint-envvar-blindspot` → closed（closed_by=ADR-0079）；11 条 open 债 `carried_log` r78 显式续记（含 #1764 OPEN 态、L2 ≈09-24 出闸临近、registerHooks 半响态）；canonical normalize 字节锁保持（ship-gate canonical-json 腿绿）。
- **upgrade-ledger v2**：待校准项消解 + 收窄断言落锚（`.scratch/grill-round-77/upgrade-ledger.md`）。
- **pathlint 登记 round-78**：`scratchDocDirs` 已含 evidence/handoffs/reports，复核无需改。
- **closeout**：`.scratch/grill-round-78/handoffs/round-78-closeout.md`（Stack 链+绿色 run URL PENDING+下轮候选+续债清单）。
- **不 bump 判定**显式入 ADR-0079 D5。

## 全绿验证（命令+输出摘要）

| 检查 | 命令 | 结果 |
|---|---|---|
| typecheck | `pnpm turbo run check` | 8/8 tasks green |
| 全量测试 | `pnpm turbo run test --continue=dependencies-successful` | 13/13 tasks green（pathlint 新测 9/9 随 store 行使） |
| install | `pnpm install` / `--frozen-lockfile` | 绿（491 条 policy 核验 / Already up to date） |
| ship-gate | `node scripts/ship-gate.mjs --skip-matrix` | **全绿**：0 腿不变量三件套、1i pathlint 317 文档净、1g closeout 3/3、pack×8、turbo 内腿、memory-eval 126/126、MCP stdio initialize、observation smoke、fail-open boot — `ship gate green — ready to tag` |
| 启动+测活 | `node apps/cli/dist/index.js --version` / `doctor` | 0.0.7 / 23 passed 2 skipped 0 failed |
| 版本控制 | `but status` | r78-impl 栈 6 commit 叠 r78-grill；工作树零脏 |

跨平台闭环：本机 Windows 全量绿；cross-OS native-smoke 4-job 矩阵按 ADR-0025 D1 委托 CI（`--skip-matrix` 语义），8 包 `check`/`test` parity 由 ship-gate 腿保证。

## 呈报事项

1. **E6 证伪（最重要）**：pnpm 11.24.0 的 lockfile 回放通道不执法龄期闸——闸内版入 lockfile 后 frozen install/fetch 静默放行。安全后果=闸只保护「主动 repin」不保护「lockfile 携闸内版」；若 CI 依赖后者须加护栏（已入 closeout 下轮候选）。
2. **E5 错误码与预登记不同**：`ERR_PNPM_LOCKFILE_CONFIG_MISMATCH`（执法点=config 一致性层先于龄期检查）——拦截成立，如实记档。
3. **WORKFLOW.md 依旧缺位**：五次先例核销（R60/R70/R75/R77/R78），以 GitButler skill+全局 but 协议等价覆盖 §4.2/§4.4。
4. **pathlint 残余盲区**：多段未知根 POSIX 路径（gitbash `/d/`、`/c/` 形）不报——已登记 ADR-0079 Consequences。
5. 落选债 11 条原名续记；#1764 仍 OPEN；外发闸两份 draft 仍待用户亲手发。

## 偏差与教训

- remediate driver 曾自食其果：自产文档的 fence-cover marker 被自家棘轮腿判 stale——已修 driver 模板（info/动作 fence 不加盖 marker）并恢复 97 动作实录。
- `node -e` 内联写 `$`/`\` 文本的教训再次验证（goal.md 既有注记）：本轮凡含正则/路径文本一律走 .mjs 文件或单引号 heredoc。
