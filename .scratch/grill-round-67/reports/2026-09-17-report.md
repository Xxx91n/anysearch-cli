# Grill Round 67 报告 — Codex 0.142.5 真宿主契约对齐 + 0.0.5 发布准备

日期：2026-09-17。任务书：`D:\Aworker\anysearch-cli\.scratch\grill-round-67\handoffs\next-round.md`。
账本：`D:\Aworker\anysearch-cli\.scratch\grill-round-67\decision-ledger.md`（D-001..D-005）。
证据根：`D:\Aworker\anysearch-cli\.scratch\grill-round-67\evidence\`。
外部 e2e 现场：`D:\Aworker\e2e-r67-codex\`（仓外）。

## 结论速览

| 票 | 状态 | 证据 |
|----|------|------|
| T1 真宿主裁决 | 绿 | `evidence\t1-defect-ledger.md` + `t1-*.stream.jsonl` |
| T2 tarball 矩阵 | 绿 | `t2-*.log` / `t2-*.stream.jsonl` |
| T3 修复 | 绿（commit `3e5087a`） | `codex-contract.test.ts` 11/11 + 全仓 test 11/11 tasks |
| T4 修复后复验 | 绿 | `t4-*.stream.jsonl`（见下） |
| T5 文档 | 绿（commits `5ae26dd`,`23ae397`） | ADR-0068/README/CHANGELOG/CONTEXT/AGENTS/codex-integration.md |
| T6 发布准备 | 绿（commit `aeb2276`） | ship-gate 全绿（净 clone 实测） |
| T7 发布执行 | 待当场授权 | tag/push/publish 未执行 |

## T1 真宿主裁决（published 0.0.4 @ codex-cli 0.142.5, Windows 11）

L0-L3/L1′ 契约形态对照（marker=随机 nonce，判读=下一 turn 指令流）：

- `{}`：NO_TOKEN，干净基线（`t1-l0*.stream.jsonl`）。
- `hookSpecificOutput.additionalContext`：4/4 送达（`t1-e1s*.stream.jsonl`）。
- **顶层裸 `additionalContext`：stub 1/8、实物 session-start 1/3 —— 不可靠**（`t1-l2s*.stream.jsonl`、`t1-l2r*.stream.jsonl`）。
- plain text：3/3；混合 `systemMessage`+envelope：1/1。
- deny 哨兵×4：envelope `permissionDecision:"deny"` 拦（router: `Tool call blocked by PreToolUse hook`，`t1-deny-env.debug.log`）；legacy `{decision:"block"}` 拦；顶层裸 deny 不拦；**exit code 2 不拦**（`t1-deny-*.debug.log`）。
- required=true MCP：硬退出，与文档一致（`t1-mcp-required.*`）。
- stdin 实测字段：`hook_event_name`/`tool_name`/`tool_input`/`tool_response`/`cwd`/`session_id`/`turn_id`/`permission_mode`（env-dump 记录，`evidence` envdump9 系列）。
- 已发布 `configs/codex/hooks.json`：`{name,command,args}` 非官方 schema——严格态 unknown-field，宽松态静默零 hooks（`t1-shippedcfg*`）。
- `-c` 无法表达 hooks：值按字符串解析报 "expected a sequence"（`t2-bisectC2.debug.log`、`t2-envdump8.debug.log`）。

## T2 tarball 矩阵 + 宿主重判

- 干净目录 `D:\Aworker\e2e-r67-codex\pkg-t`（npm --no-save tarball 安装）P1-P9：tools/list 5 件、domain-in/OOD 按 allowlist 路由、recall_memory 依赖 `ANS_PROJECT_DB`、fail-open（server down 照出结果）、重启恢复。`query_knowledge`=`adapter=none` 存量行为记 skip-with-reason。
- 关键宿主发现：**matcher=全匹配 regex**（`mcp__ansprobe__` 不命中 `mcp__ansprobe__search_web`）；**hooks env 继承 codex 进程 env**（ANS_SERVER_URL/TOKEN 实测到达，`t2-realpost-tb3` envdump 记录）；**项目 `.codex/hooks.json` 与 config.toml `[[hooks.*]]` 两层均可触发**。
- 适配器实物腿：PostToolUse 真 `mcp_tool_call` → 真 adapter → project_index +9 行（`t2-realpost-tb3`，08:38Z）。

## T3 修复（commit `3e5087a`，备份在 `.scratch\grill-round-67\backups\t3\`）

- `apps/plugin/configs/codex/hooks.json` → 官方 schema + bin 命令 + 后缀锚定 matcher + session-start `--envelope`；纳入 `gen-claude-configs.mjs` 单源（漂移守卫）。
- `apps/plugin/src/hooks/adapters/codex.ts` → 全信封输出 + `permissionDecision(Reason)` 透传（合并单信封防双 JSON）。
- `apps/plugin/src/hooks/session-start.ts` 注释纠偏（Codex 同需信封）。
- `apps/plugin/package.json` + `ans-hook-codex` bin。
- `apps/plugin/test/codex-contract.test.ts`（11 断言）；`codebuddy-contract.test.ts` 两处 codex 断言更新为新契约。

## T4 修复后复验（0.0.5 tarball @ 真宿主）

- **shipped 配置端到端**：`.codex/hooks.json` = tarball 原文 + PATH 挂 `.bin` → SessionStart `ans-hook-session-start --envelope` 触发 → 模型原文回显路由卡 `[anysearch plugin active]`（`t4-shippedcfg.stream.jsonl`）。
- **deny 端到端**：`domains/docs.toml` `urlDenylist=[evil-r67.invalid]` → 模型请求带该 URL 的 search_web → 适配器信封 deny → **mcpCalls=0 工具未执行**（`t4-deny.debug.log` router 拦截行）。
- **PostToolUse→index**：`ans-hook-codex` bin shim 经 shipped 配置触发 → +19/+10 真实结果行（`t4-shipped-postuse.stream.jsonl`；db 42 行）。
- PostToolUse ctx 同-turn 回声：0/2——同-turn 边界宿主变量（stub 通道已实证、形状 contract 已测），记 skip-with-reason；持久价值=索引副作用已证。
- `pnpm lint`/`check`/`test`/`build` 全绿（11/11 turbo tasks）。

## T5 文档（commits `5ae26dd`,`23ae397`）

- `docs\adr\0068-architecture-grill-round-67-codex-01425-host-contract-envelope.md`（Closure iv 待 T7 回填）。
- `docs\codex-integration.md`（双配置层 + MCP + hooks + 实测事实清单）。
- README：Verified agent hosts +Codex 0.142.5 行；Known Limitations +0.0.4 Codex 三项缺口；ADR 索引 regen。
- CHANGELOG 0.0.5 条目；CONTEXT.md +2 词；AGENTS.md +Codex host notes。

## T6 发布准备（commit `aeb2276`）

- 全 7 包 bump 0.0.5（含 private）；`scripts/ship-gate.mjs` 钉 0.0.4→0.0.5 同 commit。
- `pnpm pack`×4 演练：version=0.0.5、peer `workspace:*`→`0.0.5`、`ans-hook-codex` bin、官方 codex 配置全部随包。
- **ship-gate 全绿**（`/tmp/ansclean` 净 clone 实跑）：clean-tree/版本钉/task parity/静态断言/turbo check+test+build/tarball 矩阵/manifest 校验/npm install smoke/peer-optional 双装/memory-eval 126/126/MCP initialize/fail-open boot——末行 "ship gate green — ready to tag the next release"。
- GitButler 注记：仓内 `git status` 在 but workspace 下出现 phantom `D`（index 视图差异），净 clone 无此问题；ship-gate 以净 clone 为权威。

## T7 待授权事项（HARD GATE）

1. r67-grill 落地 main（路线：push 分支 + PR merge / 本地 merge——当场定）。
2. `git tag v0.0.5`（main 上）+ `git push`。
3. `release.yml` 自动：release-gate post-tag assert → publish job（OIDC TP + `--provenance`）→ 四包上 registry。
4. 发布后：`npm view` 四包 0.0.5 + `dist.attestations` + 净机 `npm i -g` 冒烟（`ans --version`/`ans doctor`）。
5. ADR-0068 Closure(iv) 实填 + 本报告 T7 段回填 + 交接更新。

## 已知遗留（非本轮修复范围）

- `query_knowledge` 为 `adapter=none` 存量 stub（Track A 观察项）。
- OOD abstain=null（allowlist 路由成立、abstain 形状另议）。
- PostToolUse ctx 同-turn 注入可靠性为宿主变量。
- hooks trust 的 `[hooks.state]` hash 生成路径未在 headless 下实证（bypass 腿已标注）。
