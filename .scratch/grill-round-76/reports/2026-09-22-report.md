# Round-76 实施报告 — 治理面静默漂移清算（T0→T1→T2 串行）

Date: 2026-09-22. Branch: `r76-impl`（GitButler 虚拟分支，叠于 `r76-grill` 之上，未 push——规则不 push 不 PR）。
Executor: 修复/开发子 Agent（Devin Desktop 会话）。

## found / fixed / deferred 三元闭环

| 类 | 条目 | 处置 |
|---|---|---|
| fixed | `defer-r71-shipgate-1g-coverage`（R70 F1：最新 round 无 closeout 时 1g leg-b fallback 到旧 round 静默报绿，四轮未修） | **fixed**：完成信号改 ADR index `Grill Round N` 登记（登记即完成）+ fail-closed 三断言 + floor=76 + 显式豁免行；registry 条目核销 closed_by=ADR-0077 |
| fixed | `docs/deferred-registry.json` 外部工具两次整文件重排（1→8、1→4 空格） | **fixed**：`stepGovernedJsonCanonical` 字节锁（canonical `JSON.stringify(_,null,1)+'\n'`、不自修、normalize 指令、`note` 自文档）；立项事实入档 `gov-r76-registry-canonical-lock` |
| found | canonical 断言不测「键序重排成 canonical 形态」（canonicalize(文件)≡文件 的固有面） | **deferred（显式接受）**：ADR-0077 D5 记档——两次事故形态均为整文件缩进重排，不在此面；翻案条件已写 |
| found | 断言 (c)「空推导→红」在真 gate 被 step 1b（index regenerate-and-diff）结构先行拦截 | **deferred（记档）**：模块层 probe 行使 + 接线断言兜底；若未来 1b 改序需补腿级实测 |
| deferred（续债，原名+carried_log 显式记） | `defer-r71-transformers-undeclared-dep` / `defer-r71-provider-serverside` / `defer-r72-dsh-plugin-npm-publish` / `defer-r72-dsh-native-tools` / `defer-r72-dsh-web-interactive-matrix` / `defer-r73-dsh-event-rename` / `defer-r74-logo-bitmap-matrix` / `defer-r75-registerhooks-esm-arm` / `defer-f16-macos-native-crash` / `defer-f17-quarantine-ids` / `defer-anysearch-domain-ownership` | 显式续债——本轮落选，11 条各附 `carried_log: {at:2026-09-22, round:76, note:<理由>}`；入 closeout「Known risks / deferred」段 |

## T0 — registry 格式锁（D-003）✅

- `scripts/governed-json.mjs` 新模块：`canonicalJsonBytes`（canonical=`JSON.stringify(JSON.parse(src),null,1)+'\n'`）/`firstDifferingLine`（首个不同字节→行号）/`normalizeCommand`/`governedJsonViolation`（非法 JSON 独立报错，normalize 指引只在合法但字节不匹配时给）。
- `scripts/ship-gate.mjs` step 1 内 `stepGovernedJsonCanonical()`（未开新编号腿）+ 硬编码 `CANONICAL_JSON_FILES=["docs/deferred-registry.json"]`（注释含 ADR-0077 + n≥5/包级自服务迁移触发条件）。
- registry 顶层 `note` 追加锁定声明（含 normalize cmd），以 canonical 形态一次定稿。

## T1 — 1g 修复（D-002）✅

- `scripts/closeout-coverage.mjs` 新模块：`CLOSEOUT_COVERAGE_FLOOR=76`（规则生日）/`isCloseoutName`/`parseRegisteredRounds`（BEGIN/END 块内行形状白名单，未知行 fail-loud，file/title 轮号冲突+重复登记均报错）/`scanRoundDirs`/`assessCloseoutCoverage`（三断言+豁免+计数）。
- `stepHandoffCloseoutLint` leg-b 重写：登记信号驱动，字段 lint 面不动（仍只最新含 closeout dir）；成功输出 `N registered / M completed round(s) at floor 76`。

## T2 — 文书收口（D-001/D-005）✅

- `docs/adr/0077-architecture-grill-round-76-silent-drift-governance-liquidation.md` 落档 + `gen-adr-index --write` regen（77 条，`--check` 绿）。
- registry：`defer-r71-shipgate-1g-coverage` 核销（closed_by=ADR-0077）；`gov-r76-registry-canonical-lock` 立项记录；11 条落选债 `carried_log`；canonical 形态保持。
- `CONTEXT.md`：`Carried Log（显式续债条）` 实现期新词补记（grill 六词已在册）。
- `CHANGELOG.md`：`Unreleased — ADR-0077 r76` 条目（不 bump 判定随 ADR D6 记档）。
- pathlint：round-76 文档全在已登记 doc dirs（handoffs/reports/evidence/drafts），无需改 `scratchDocDirs`——自证=本轮文档过 lint。
- handoff：`handoffs/round-76-closeout.md` + `next-round.md`（#1764 观察哨续挂、外发闸待用户发、落选债承接、翻案哨兵）。

## 验收对账（每条附可复跑命令）

| 验收锚 | 证据 | 复跑 |
|---|---|---|
| T0 注入非 canonical→fail 报 first-differs+指令 | 注入 8 空格重排→`[fail] canonical-json: docs/deferred-registry.json is not in canonical form (first differs at line 2) — normalize: node -e "const f='docs/deferred-registry.json';const fs=require('fs');fs.writeFileSync(f,JSON.stringify(JSON.parse(fs.readFileSync(f,'utf8')),null,1)+'\n')" — governed list: CANONICAL_JSON_FILES in scripts/ship-gate.mjs (ADR-0077)`，exit=1 | `evidence/t0-canonical-inject.log`；`node scripts/ship-gate.mjs --quick`（干净树） |
| T0 恢复→转绿 | normalize 指令亲跑恢复 15065B canonical；后续 gate run step 1t `[pass] canonical-json: 1 governed file(s) byte-identical` | `node -e "const f='docs/deferred-registry.json';const fs=require('fs');fs.writeFileSync(f,JSON.stringify(JSON.parse(fs.readFileSync(f,'utf8')),null,1)+'\n')"` |
| T1 断言 (a) 实测红 | 登记先行态（ADR-0077+index regen 已提交、closeout 未写）→`[fail] closeout-coverage: 1 violation(s): round 76: registered in docs/adr/index.md but .scratch/grill-round-76/handoffs/ has no closeout doc — write the closeout or remove the registration`，exit=1 | `evidence/t2-assertion-a.log` |
| T1 断言 (b) 实测红 | 临时 commit 注入 `.scratch/grill-round-77/handoffs/round-77-closeout.md`（未登记+有 closeout）→`round 77: closeout doc(s) on disk but no Grill Round 77 registration in docs/adr/index.md — index<->.scratch drift`，exit=1；commit 已 discard 未留史 | `evidence/t2-assertion-b.log` |
| T1 断言 (c) 空推导→红 | 模块层行使：`assessCloseoutCoverage({dirs:[D(76,true)],indexText:IDX([])})` → `derived zero Grill Round registrations ... a gate with nothing to check is a failure`；parser 未知行/冲突/重复/缺 marker 全 fail-loud | `node --import tsx packages/store/test/closeout-coverage.test.mjs`（26/26） |
| T1 豁免行可见 | 在飞态实测：`[skip] awaiting closeout: round 76 (ADR not yet registered)` + `[pass] closeout-coverage: 0 registered / 0 completed round(s) at floor 76` | `evidence/t1-inflight-exemption.log` |
| T1 成功输出带计数 | 登记+closeout 齐备态：`[pass] closeout-coverage: 1 registered / 1 completed round(s) at floor 76` | `evidence/t2-registered-green.log` |
| T1 字段 lint 面不动 | 在飞态 lint round-75 closeout（最新含 closeout dir）、登记后 lint round-76 closeout——均 `carry 绿色 run URL + Stack and cite a run on this round's history` | 同上两 log |
| 编译通过 | `turbo check` 8/8 绿 | `pnpm run check` |
| 每个平台 test 闭环 | `turbo test` 13/13 tasks 绿——两新测试随 turbo test 行使（`closeout-coverage.test: 26 passed`、`governed-json-canonical.test: 19 passed`，CI ubuntu+windows 双平台同 lane） | `pnpm run test` |
| 打包通过 | ship-gate step 4：8 包 `pnpm pack` 全绿（tgz 实物+manifest 断言） | `node scripts/ship-gate.mjs --skip-matrix` |
| 启动并测活软件进程 | `node apps/cli/dist/index.js --version`→`0.0.7`；`doctor`→`25 passed, 0 skipped, 0 failed`（含 `[OK] vector arm`）；ship-gate step 8/9 MCP stdio initialize `server=anysearch v0.0.7` + env-scrubbed fail-open boot 绿 | `node apps/cli/dist/index.js doctor` |
| ship-gate 全绿 | `--quick` 三跑（在飞豁免/断言红/登记绿）+`--skip-matrix` 终跑 9/9 步全绿 | `node scripts/ship-gate.mjs --skip-matrix` |
| but commit 干净 | `git status --porcelain` 空；probe 临时 commit 均 discard 未留史 | `but status` |

## 诚实注记

- (c) 空推导断言在真 gate 结构上不可达：step 1b（`gen-adr-index --check`，index 与 git tree 强一致）先于 1g 拦截任何 index 变形；模块层已行使 + ship-gate 接线断言兜底。
- 两次 probe 均走「临时 commit → 真 gate 行使 → `but discard`」路线（clean-tree 不变式要求门在已提交态运行）；fixture 文件未进任何留存 commit。
- 报告与 closeout 引用的 ship-gate 绿证据为 `--quick`/`--skip-matrix` 本地跑；cross-OS native 矩阵由 CI `native-smoke.yml` 4-job 覆盖（本栈未 push，run URL=PENDING）。
