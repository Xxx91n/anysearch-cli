# Grill Round 66 — Report (2026-09-17)

身份：修复/开发子 Agent。任务书 `.scratch/grill-round-66/handoffs/next-round.md`，账本同目录 `decision-ledger.md`（D-001..D-007）。覆盖票 T2 收尾→T3→T4（部分）→T5→T6；T7 停在授权点。

## 声明→证据映射

### T3 修复（红→绿 闭环）

| 声明 | 复跑命令 | 证据 |
|------|----------|------|
| adapter/session-start 决策键全量入 `hookSpecificOutput` 信封（顶层实测被吞） | `cd apps/plugin && pnpm vitest run` → 126/126 绿（含新 claude-contract 10 测）；`pnpm exec tsc --noEmit` 0 错；`pnpm lint` clean | apps/plugin/src/hooks/adapters/claude.ts、src/hooks/session-start.ts、test/claude-contract.test.ts |
| configs/claude/hooks.json 重写为官方 settings schema，命令=bin `ans-hook-claude`/`ans-hook-session-start` | `node apps/plugin/scripts/gen-claude-configs.mjs` 生成；`cat configs/claude/hooks.json` | apps/plugin/configs/claude/hooks.json |
| plugin 骨架 `.claude-plugin/plugin.json`+`hooks/hooks.json`+`.mcp.json` 单源生成+漂移守卫 | 同上；契约测 `node --test test/claude-contract.test.ts` | apps/plugin/scripts/gen-claude-configs.mjs |
| 通用契约测改为 bin→dist 解析（不假设目标串含路径） | `node --test test/codebuddy-contract.test.ts` 20/20 | test/codebuddy-contract.test.ts |

### T4 修复后复跑（t3-pack tarball → npm-global 安装）

| 声明 | 复跑命令 | 证据 |
|------|----------|------|
| 出厂 template（官方 schema+bin）→ anysearch SessionStart 钩子真实触发，信封实发 | `node scripts/write-settings.mjs template && node scripts/probe-claude.mjs t4-p5-realcall "<prompt>"` → transcript 中 `hook_response` 含 `{"hookSpecificOutput":{"hookEventName":"SessionStart","additionalContext":"[anysearch plugin active]…"}}` | evidence/t4-p5-realcall.stream.jsonl |
| `claude plugin validate` repo+installed 全过 | `claude plugin validate "C:/Users/Administrator/AppData/Roaming/npm/node_modules/@anysearch-cli/plugin"` → `✔ Validation passed`（0 error/0 warn） | 终端输出（可复跑） |
| `--plugin-dir` 实测加载成功：plugin 侧 MCP 连接 + SessionStart 钩子触发 | `probe-claude.mjs t4-p11-plugindir --nomcp --plugindir <pkg>` → init `plugin:anysearch:anysearch:connected`；plugin hook_response 同信封 | evidence/t4-p11-plugindir.stream.jsonl |
| `${CLAUDE_PLUGIN_ROOT}` 本机展开正常（与上游 #16116 链结论矛盾——本机 2.1.251 实测可用） | 同上 transcript hook_response | 同上 |
| validate 过程抓出两处字段级缺陷并修复 | `repository` 须 string（npm 对象形态报错）；SKILL.md 须 frontmatter | gen-claude-configs.mjs、skills/anysearch/SKILL.md |

### T5 文档

| 声明 | 文件 |
|------|------|
| ADR-0067（含 Closure 回填位，PENDING 标注如实） | docs/adr/0067-*.md |
| Claude 接线文档（实测宿主事实清单+settings/plugin 双通路+fail-open） | docs/claude-integration.md |
| README verified-hosts 增 Claude 2.1.251 行（settings=live-verified，plugin=experimental）；Known Limitations 补两条 0.0.3 Claude 缺口 | README.md |
| CHANGELOG r66 条目 | CHANGELOG.md |
| CONTEXT 词块 | 任务书预置（Grill Round 66 — Terms 段已存在，7 条） |

### T6 发布工程（未发布——授权点）

| 声明 | 复跑命令 | 证据 |
|------|----------|------|
| 全包 bump 0.0.4（7 包统一） | `node -e "…"` 脚本；`grep '"version"' */package.json` | apps/*/package.json、packages/*/package.json |
| release.yml 增 publish job：OIDC trusted publishing（`id-token:write`、npm≥11.5.1、provenance、tag push 触发、gate 绿后才发） | `python -c "import yaml;yaml.safe_load(open('.github/workflows/release.yml'))"` → valid | .github/workflows/release.yml |
| TP 逐字配置指令可照抄 | docs/publishing.md §OIDC trusted publishing | docs/publishing.md |
| dry-run 打包四包 0.0.4：peer `workspace:*`→`0.0.4` 重写、骨架+skill+官方模板全在 tarball | `pnpm pack --pack-destination .scratch/grill-round-66/t6-pack` + `tar -tf`/`tar -xOf` 检查 | .scratch/grill-round-66/t6-pack/*.tgz |

## 阻塞项（基础设施，非产品）

**Claude 模型代理 `127.0.0.1:15721` 宕机 ≥45min**（ECONNREFUSED，`ANTHROPIC_BASE_URL` 指向它，`PROXY_MANAGED` 由外部进程托管）。症状：`claude -p` 全部 `system/api_retry` 后超时——init/MCP/hook 链路完整产出，仅模型调用不可达。

**需用户恢复该代理服务**后补跑（10–15 min）：
1. template 注入→模型引用路由卡（injection→awareness 闭环最后一环）；
2. 真实 `search_web` 成功调用→PostToolUse→projectIndex 行增量；
3. P9 第二跑（方差）；
4. plugindir 模型级会话。

## 当前状态四态汇总

- **core（必绿）**：P1 MCP 注册+5 工具、P2 域内实调、P6 hooks 执行（marker 法）、P6x 信封契约（三腿哨兵）、P7 fail-open、P8 research、P11 validate+plugin-dir——**全绿**
- **host-variable**：P9 双跑（0.0.3 第一跑已得，第二跑等代理）、P5 index 增量（等代理）
- **experimental**：plugin 通路 validate+load 已绿，模型级会话等代理
- **expected-red 闭环**：ER-1/2/3 全部转绿（含 validate 抓出的字段级新缺陷）

## 授权点（T7 硬停）

未做任何 publish/tag/push。T7 需用户：①恢复 15721 代理；②npmjs.com 四包配 trusted publisher（docs/publishing.md 指令逐字）；③当场授权 tag v0.0.4+push+publish。

## Git 状态

but 栈：`r66-grill` → `r66-t1-baseline`(xyt) → `r66-t2-tarball-matrix`(pty) → `r66-t3-fixes`(yol,wvq,rpo) → `r66-t6-release`(uzr)；旁支 `r66-t5-docs`(qsp)。工作区干净，无 push。
