# Round-75 实施报告 — transformers 未声明外部债判定性清算（T0–T3 串行）

Date: 2026-09-22. Branch: `r75-grill`（GitButler 虚拟分支，未 push——规则不 push 不 PR）。
Executor: 修复/开发子 Agent（Devin Desktop 会话）。

## found / fixed / deferred 三元闭环

| 类 | 条目 | 处置 |
|---|---|---|
| found | `defer-r71-transformers-undeclared-dep` 删除条件疑似半响（4.x 收 onnxruntime-node/web 进直接 deps） | **fixed(判定)**：条件未达成——目标 specifier `onnxruntime-common` 仍顶层 bare require 且 manifest 未声明；patch 续存 + 证据刷新（registry status 续 open） |
| found | ESM 入口缺口（`import()` 消费者绕开 CJS patch） | **deferred**：`defer-r75-registerhooks-esm-arm` 具名重返票（三触发器入册）；静态护栏替代运行时臂 |
| found | workspace 内 pnpm 11 不物化 optionalDependencies 到包自身 node_modules（锁文件有、.pnpm 有、importer link 缺） | **fixed(护栏适配)**：配对断言走 importer-anchor → .pnpm store 扫描双锚；记档于 evidence t2 注记（fail-open 语义下从未破测试，本断言是首个在乎者） |
| found | pnpm 默认配置经 `.pnpm/node_modules` 虚拟店 hoist **掩盖** ghost dep（hoistPattern=* 默认开启） | **fixed(证据注记)**：T0 Leg D 实物记录；断裂面收窄为 hoist:false / pnpm dlx / global 形态——上游文稿已按此精确表述 |
| deferred（续债，原名不动） | `defer-r71-shipgate-1g-coverage` / `defer-r71-provider-serverside` / `defer-r72-dsh-plugin-npm-publish` / `defer-r72-dsh-native-tools` / `defer-r72-dsh-web-interactive-matrix` / `defer-r73-dsh-event-rename` / `defer-r74-logo-bitmap-matrix` / `defer-f16-macos-native-crash` / `defer-f17-quarantine-ids` / `defer-anysearch-domain-ownership` | 显式续债——本轮落选，入 closeout「Known risks / deferred」段；registry 条目原状未动 |
| deferred（核销） | `origin/r71-grill` 已消失 | `git ls-remote origin`（2026-09-22）实证 remote 仅剩 `main` + PR refs——上轮列报项核销，无操作 |

## T0 — 活证据补强（D-002）✅

pnpm 隔离 scope fixture（机器临时目录，未提交；每 fixture `pnpm-workspace.yaml` 置 `hoist: false`，`.pnpm/node_modules` 实证 0 项）：

- **Leg A**：`@huggingface/transformers@4.3.0` 单依赖 → `node -e "require('@huggingface/transformers')"` → **`Error: Cannot find module 'onnxruntime-common'`，`transformers.node.cjs:13520:33`，`code: 'MODULE_NOT_FOUND'`，exit=1**（逐字摘录见 transcript）。
- **Leg B**（负对照）：`3.8.1` 无 patch 无自声明 → 同款 `Cannot find module 'onnxruntime-common'`，exit=1。
- **Leg C**（对照绿）：`3.8.1` + 自声明 `onnxruntime-common@1.21.0` + 逐字 CJS 移植的 scoped `_resolveFilename` patch → `REQUIRE-SUCCEEDED`，解析落 fixture `.pnpm/onnxruntime-common@1.21.0`，exit=0。
- **Leg D**（诚实注记）：4.3.0 默认配置（virtual-store hoist 活）→ REQUIRE-SUCCEEDED——掩盖面记档，申报文稿据此收窄表述。

实物：`.scratch/grill-round-75/evidence/t0-pnpm-isolated-scope.log`（逐字 transcript）+ `t0-pnpm-isolated-scope.md`（verdict 表）。

## T1 — 上游申报文稿（D-002/D-004）✅（待用户发）

- `drafts/pr-1764-comment.md`——#1764 评论全文（英文）：4.3.0 隔离 scope 复现逐字栈 + 默认 hoist 掩盖 nuance + 「消费侧修法均不旅行」论证（packageExtensions/patchedDependencies/自声明错 scope/dlx 不适用）+ 下游 ≥4 项目造轮子清单（episodic-memory#105 / mastra packageExtensions 注释原文 / GitNexus#2069 ~300 行 fallback / 本项目 scoped patch）+ 关联 #1087 + 礼貌推合并优先级。
- `drafts/issue-1087-comment.md`——#1087 短评指路（可选件，服务后来检索者）。
- **先读 #1087 全部评论已做**（gh issue view --comments 2026-09-22）：xenova 以 repo 内 publicHoistPattern 结案、uwuclxdy 4.2.0 dlx 复现补刀、nico-martin 指路 #1701——文稿不复述已驳回论点，落点=「published 消费者仍裸奔」+「修复已在 #1764 在飞」。
- 红线遵守：未用 gh 代发、未自交 PR、未开新 issue。递交状态：**待用户亲手发**（轮内合法终态）。

## T2 — 静态护栏双件（D-003）✅

`packages/embedding/test/transformers-ghost-dep.test.ts`（repo 无 eslint——选型落 node:test 断言，随 `turbo test` 行使）：

- **(a) 禁裸引**：`src/` 全树扫描，禁 `import`/`from`/`export … from`/动态 `import()` 直引 `@huggingface/transformers`；注释与 `typeof import(…)` 类型查询豁免（src/index.ts:13 既有合法用例不误报）。
- **(b) 版本配对**：`optionalDependencies["onnxruntime-common"]` ≡ effective `onnxruntime-node` 内嵌 `dependencies["onnxruntime-common"]`（当前 1.21.0 ≡ 1.21.0）；双锚解析（importer link 优先、.pnpm store 扫描兜底——见 found 表 pnpm 物化缺口）。
- **自证反例红**：探针文件注入裸 `import … from` → AssertionError 红、删后绿；manifest 钉版改 `0.0.0-WRONG` → AssertionError 红、还原后绿（`git diff` 空=逐字还原）。实物 `evidence/t2-guardrails-selfproof.log`。
- 红线遵守：`src/index.ts` patch 本体零改动（`git diff` 该文件为空）。

## T3 — 文书收口（D-002/D-003/D-006）✅

- `docs/deferred-registry.json`：`defer-r71-transformers-undeclared-dep` 三字段刷新（upstream_issue 指 #1087 CLOSED+#1764 OPEN 链接 / evidence 4.3.0 解剖+运行时复现+配对纪律 / monitoring_channel 改 watch >4.3.0+#1764 merge），status **续 open**；新增 `defer-r75-registerhooks-esm-arm`（13 条）。
- `docs/adr/0076-*.md`：ADR-0076 落档——判定逻辑（Half-Fired Trigger）+ 申报策略（advocate 在飞修复）+ 静态护栏教义 + 版本配对契约 + **不发布判定**（无发布态代码增量→不 bump，显式记录）。
- `docs/adr/index.md`：gen-adr-index regen（76 条，`--check` 绿）。
- `CHANGELOG.md`：`Unreleased — ADR-0076 r75` 条目按 r74 docs-only 轮惯例落（判定=需要条目但不 bump）。
- `scripts/ship-gate-pathlint.config.json`：`scratchDocDirs` 登记 `drafts`。
- CONTEXT.md：R75 词块（Half-Fired Trigger / Travelling Guardrail / Static Invariant Guard / Version-Pairing Contract / Advocate In-Flight Fix）已于 grill 阶段落盘，本轮核验在册，无新增凝结词。

## 验收对账（每条附可复跑命令）

| 验收锚 | 证据 | 复跑 |
|---|---|---|
| 编译通过 | `turbo check` 8/8 绿 | `pnpm run check` |
| 打包通过 | ship-gate step 4：8 包 `pnpm pack` 全绿（tgz 实物产出+manifest 断言） | `node scripts/ship-gate.mjs --skip-matrix` |
| 启动并测活软件进程 | `ans --version`=0.0.7；`ans doctor` 25/0/0（含 `[OK] vector arm (present …)`）；ship-gate step 8/9：MCP stdio initialize `server=anysearch v0.0.7` + env-scrubbed fail-open boot 绿 | `node apps/cli/dist/index.js doctor` |
| 每个平台 test 闭环 | `turbo test` 13/13 tasks 绿（embedding 2/2 含新护栏、store/kernel/retriever/cli/mcp/plugin/dsh-plugin 全绿）；新断言随 `turbo test` 行使非摆设 | `pnpm run test` |
| ship-gate 绿 | 9/9 步全绿 `[pass] ship gate green — ready to tag the next release`（含 step 0 三条不变量、1g closeout-lint、1h parity、1i pathlint、adr-index --check） | `node scripts/ship-gate.mjs --skip-matrix` |
| T0 锚 | transcript 实物 + 4.3.0 失败逐字 + 3.8.1 对照绿 | evidence/t0-pnpm-isolated-scope.log |
| T1 锚 | 文稿实物 + T0 引用 + #1087/#1764 关联 + 待用户发记录 | drafts/*.md |
| T2 锚 | 双断言实物 + 反例自证红 + 全套件绿 | evidence/t2-guardrails-selfproof.log |
| T3 锚 | 四段收口齐：实证/申报/护栏/文书 | 本表 + ADR-0076 §Closure evidence |

## 审计返工记录（r75-audit `mpz` 打回，轻量）

| 项 | 级别 | 处置 |
|---|---|---|
| F-1 ADR-0076 D4 伪述（写「no CHANGELOG entry」但实物落了 Unreleased 节） | major | **fixed**：D4 改为「no version bump and no release; CHANGELOG Unreleased 节按 r74 惯例落」——改 ADR 不改实物 |
| F-2 registry 顶层 `updated` 滞留 2026-09-20 | minor | **fixed**：bump 2026-09-22 |
| F-3 护栏 regex 三逃逸（模板串引号/子路径 specifier/同行字符串内 `//` 掩行） | 加固 | **fixed**：引号类补 `` ` ``、specifier 尾接受 `/`、`//` 字符串盲区记档为接受限制（regex-literal 歧义需真 tokenizer）；新增 in-test 违例语料 9 红 + 合法语料 5 绿——逃逸自证从一次性升为每次 `turbo test` 行使 |
| F-4 死导入 `statSync`、`SPEC.replace` ×4、`export *` 冗余 pattern | 加固 | **fixed**：三处清理（export-* 由 `from` 腿覆盖） |
| F-5/F-6 `import type` 从严、`.pnpm` 首匹配/魔数 6/SKIP 三因合一 | backlog | **记档**：测试注释留痕，行为不改 |

返工后复跑：`pnpm exec node --import tsx --test test/transformers-ghost-dep.test.ts` → guard (a) clean + (a′) 9 violation flagged/5 legal clean + (b) 1.21.0≡1.21.0，pass 1 fail 0。

## 过程违规自报

无。WORKFLOW.md §4.2 引用件全盘搜索不存在（R60/R70 先例核销）——以 gitbutler SKILL + 全局 but 协议等价覆盖：GitButler 栈 `r75-grill`、禁裸 git 写、commit 中文带票号、不 push 不 PR。

## 备注

- `docs/deferred-registry.json` 会话中曾被外部进程整文件重排版（1→8 空格缩进）；语义 diff 实证零内容改动后按 `JSON.stringify(·,1)` 原样恢复，提交 diff 保持最小（+16/−3）。
- 审计返工期间该文件再次被重排为 4 空格缩进（整文件翻滚，语义已核净：+1 条目/1 条目刷新/updated bump）；审计窗第二次按 `JSON.stringify(·,1)` 恢复，终态 diff 回到最小面（+17/−4）。
- fixture 目录未提交（机器临时目录）；transcript 为唯一归档物——.log 不受 pathlint 扫面（扫面仅 .md），.md 侧全 repo-relative 引用。
